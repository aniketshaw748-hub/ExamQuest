// Authored Explanation-Ladder lessons for Design & Analysis of Algorithms.
// Standard CS material (the verified PYQ answers still come from daa.json untouched).

export const TEACH_DAA = {
  ch1: [
    {
      id: "complexity-basics",
      title: "Time and Space Complexity",
      explainer: "complexity",
      what: "Algorithm analysis measures the time and space an algorithm needs as a function of input size $n$, independent of machine or language.",
      why: "You compare algorithms before coding them. A priori (theoretical) analysis predicts cost from the algorithm itself; a posteriori measures an actual run.",
      intuition: "Count the elementary operations (comparisons, assignments) as $n$ grows. The growth rate, not the exact count, is what scales.",
      analogy: {
        icon: "ruler",
        label: "Measure by growth",
        text: "Like rating cars by their fuel-economy curve, not one trip: you care how cost rises with distance ($n$), not a single number.",
        breaks: "Real runs vary with hardware; asymptotic cost abstracts that away.",
      },
      steps: [
        "Pick the input size $n$.",
        "Count elementary operations as a function of $n$.",
        "Time complexity = operation count; space complexity = memory cells used.",
        "Distinguish best, worst and average case.",
        "A priori = theoretical formula; a posteriori = measured run.",
      ],
      example: "Linear search does up to $n$ comparisons: worst case $n$, best case 1, average about $n/2$, so $\\Theta(n)$. Space is $S(X)=C(X)+I(X)$, fixed plus instance part.",
      misconception: "Time complexity is not seconds; it counts operations as a function of $n$. Faster hardware changes the constant, not the growth class.",
      rule: "Analyse by growth in $n$: count operations, separate best/worst/average, ignore machine constants.",
    },
    {
      id: "asymptotic-notation",
      title: "Asymptotic Notation",
      explainer: "bigo",
      what: "Asymptotic notation classifies growth: $O$ is an upper bound, $\\Omega$ a lower bound, $\\Theta$ a tight bound, with $o$ and $\\omega$ the strict versions.",
      why: "For large $n$ only the dominant term matters. Notation lets you say '$\\Theta(n\\log n)$' and compare algorithms without machine-specific detail.",
      intuition: "Drop constants and lower-order terms. $3n^2+5n+7$ is $\\Theta(n^2)$ because $n^2$ dwarfs the rest as $n$ grows.",
      analogy: {
        icon: "podium",
        label: "Who wins as n grows",
        text: "Each complexity class is a runner; as the track ($n$) lengthens the slower-growing function always wins, whatever head start (constant) the others had.",
        breaks: "For small $n$ a 'worse' class can be faster; asymptotics only describe the large-$n$ limit.",
      },
      steps: [
        "$f=O(g)$: $f \\le c\\,g$ for large $n$ (upper bound).",
        "$f=\\Omega(g)$: $f \\ge c\\,g$ (lower bound).",
        "$f=\\Theta(g)$: both hold, a tight bound.",
        "$o(g)$ is a loose upper bound ($f/g\\to 0$); $\\omega(g)$ a loose lower bound.",
        "Keep only the dominant term; drop constants.",
      ],
      example: "$O(\\log n) < O(n) < O(n\\log n) < O(n^2) < O(2^n)$. The lower bound for any comparison sort is $\\Omega(n\\log n)$, so merge sort at $\\Theta(n\\log n)$ is optimal.",
      misconception: "$O$ is only an upper bound, not 'exactly'. Calling an $O(n)$ algorithm '$O(n^2)$' is technically true but uselessly loose; use $\\Theta$ for a tight claim.",
      rule: "$O$ upper, $\\Omega$ lower, $\\Theta$ tight. Compare by dominant term; comparison sorts are $\\Omega(n\\log n)$.",
    },
    {
      id: "masters-theorem",
      title: "The Master Theorem",
      explainer: "master",
      what: "The Master Theorem solves divide-and-conquer recurrences $T(n)=aT(n/b)+f(n)$ by comparing $f(n)$ with $n^{\\log_b a}$.",
      why: "Most divide-and-conquer algorithms (merge sort, binary search, Strassen) give this recurrence. The theorem reads off the answer in one step instead of unrolling.",
      intuition: "Compare the split/combine work $f(n)$ against the leaf work $n^{\\log_b a}$. Whichever dominates sets the cost; if they tie, multiply by a log.",
      analogy: {
        icon: "ladder",
        label: "Top-heavy or bottom-heavy",
        text: "A recursion tree is heavy at the leaves ($n^{\\log_b a}$) or at the root ($f(n)$). The heavier end decides the total.",
        breaks: "It needs $f(n)$ to behave polynomially; some recurrences (like $f(n)=n/\\log n$) fall in the gaps and need other methods.",
      },
      steps: [
        "Write the recurrence as $T(n)=aT(n/b)+f(n)$.",
        "Compute the watershed $n^{\\log_b a}$.",
        "Case 1: $f(n)$ polynomially smaller $\\Rightarrow T=\\Theta(n^{\\log_b a})$.",
        "Case 2: $f(n)=\\Theta(n^{\\log_b a}\\log^k n) \\Rightarrow T=\\Theta(n^{\\log_b a}\\log^{k+1} n)$.",
        "Case 3: $f(n)$ polynomially larger and regular $\\Rightarrow T=\\Theta(f(n))$.",
      ],
      example: "Merge sort $T(n)=2T(n/2)+n$: here $n^{\\log_2 2}=n$ and $f(n)=n$, Case 2 with $k=0$, so $T(n)=\\Theta(n\\log n)$. Binary search $T(n)=T(n/2)+1=\\Theta(\\log n)$.",
      misconception: "The Master Theorem only fits $aT(n/b)+f(n)$ with constant $a,b$. Recurrences like $T(n)=T(n-1)+n$ are not divide-and-conquer; unroll those instead.",
      rule: "Compare $f(n)$ with $n^{\\log_b a}$: smaller, equal-times-log, or larger picks the case.",
    },
    {
      id: "recurrence-methods",
      title: "Recursion-Tree and Substitution",
      explainer: "rectree",
      what: "When the Master Theorem does not apply, solve recurrences by the recursion-tree method or the substitution (guess-and-induct) method.",
      why: "Many recurrences fall outside the Master form. These two methods handle the rest and build the intuition behind the Master Theorem.",
      intuition: "Recursion tree: draw the cost at each level and sum the series. Substitution: guess the bound, then prove it by induction.",
      analogy: {
        icon: "stack",
        label: "Sum the levels",
        text: "Unfold the recursion into a tree; each level is a row of work. Add the rows (often a geometric or arithmetic series) to get the total.",
        breaks: "Substitution needs a correct guess; a wrong guess can seem to work until the induction fails on the constants.",
      },
      steps: [
        "Recursion tree: expand the recurrence level by level.",
        "Find the per-level cost and the number of levels.",
        "Sum the series across all levels.",
        "Substitution: guess $T(n)=O(g(n))$.",
        "Prove the guess by induction, fixing the constant.",
      ],
      example: "$T(n)=2T(n/2)+n$ has a recursion tree of $\\log n$ levels each costing $n$, total $\\Theta(n\\log n)$. Substitution then verifies $T(n)\\le cn\\log n$ by induction.",
      misconception: "A recursion tree gives a guess, not always a proof; confirm it with substitution. And the per-level cost can change between levels, do not assume it is constant.",
      rule: "Recursion tree to sum level costs; substitution to guess then prove by induction.",
    },
  ],
  ch3: [
    {
      id: "graph-representations",
      title: "Representing Graphs",
      explainer: "gmatrix",
      what: "A graph is stored as an adjacency matrix ($V\\times V$ of 0/1) or an adjacency list (each vertex lists its neighbours).",
      why: "The representation sets the cost of every later operation. Matrices give $O(1)$ edge lookups but $O(V^2)$ space; lists are compact for sparse graphs.",
      intuition: "Matrix = a full grid of who-connects-to-whom. List = each vertex carries a small list of its neighbours, cheap when edges are few.",
      analogy: {
        icon: "grid",
        label: "Grid vs contact list",
        text: "Adjacency matrix is a seating grid marking every pair; adjacency list is each person's contacts, only the people they actually know.",
        breaks: "Matrices waste space on sparse graphs; lists make 'is there an edge u-v?' slower ($O(\\deg)$ instead of $O(1)$).",
      },
      steps: [
        "Adjacency matrix: $A_{ij}=1$ if edge $ij$ exists.",
        "Symmetric for undirected graphs; space $O(V^2)$.",
        "Adjacency list: each vertex stores its neighbours.",
        "Space $O(V+E)$, great for sparse graphs.",
        "Choose by density: dense $\\Rightarrow$ matrix, sparse $\\Rightarrow$ list.",
      ],
      example: "A graph with 1000 vertices and 2000 edges wastes a million matrix cells but needs only about 3000 list entries. Edge query is $O(1)$ in a matrix, $O(\\deg v)$ in a list.",
      misconception: "Adjacency-matrix space is $O(V^2)$ regardless of edge count, so it is wasteful for sparse graphs. Match the representation to graph density.",
      rule: "Matrix: $O(V^2)$ space, $O(1)$ edge test. List: $O(V+E)$ space, best for sparse graphs.",
    },
    {
      id: "bfs-dfs",
      title: "Breadth-First and Depth-First Search",
      explainer: "graph",
      what: "BFS explores a graph level by level using a queue; DFS goes as deep as possible first using a stack or recursion.",
      why: "These are the backbone of graph algorithms: BFS finds shortest paths in unweighted graphs; DFS detects cycles, finds connected components and orders for topological sort.",
      intuition: "BFS ripples outward like a wave from the source. DFS dives down one path until stuck, then backtracks.",
      analogy: {
        icon: "map",
        label: "Flood vs explore",
        text: "BFS floods the nearest rooms first (a wave); DFS sends one explorer down a corridor to the end, then backtracks to the last junction.",
        breaks: "BFS shortest paths only hold for unweighted edges; with weights you need Dijkstra.",
      },
      steps: [
        "BFS: from the source, visit all neighbours, then their neighbours, using a queue.",
        "BFS gives shortest path counts in unweighted graphs.",
        "DFS: follow one edge as deep as possible, backtrack at dead ends, using a stack or recursion.",
        "Both run in $O(V+E)$ with adjacency lists.",
        "Mark visited vertices so you never revisit.",
      ],
      example: "On an unweighted graph, BFS from $s$ labels every vertex with its hop-distance from $s$. DFS on the same graph reveals back edges, which signal cycles.",
      misconception: "BFS and DFS visit the same vertices but in different orders; only BFS gives shortest paths (in unweighted graphs). DFS order is not by distance.",
      rule: "BFS = queue, level order, unweighted shortest paths. DFS = stack/recursion, deep first, cycles and components. Both $O(V+E)$.",
    },
    {
      id: "spanning-trees-mst",
      title: "Spanning Trees and MST",
      explainer: "tree",
      what: "A spanning tree connects all $V$ vertices with $V-1$ edges and no cycle. A minimum spanning tree (MST) is the cheapest such tree by total edge weight.",
      why: "MST answers 'connect everything as cheaply as possible' (networks, roads). Kruskal and Prim both build it greedily and provably optimally.",
      intuition: "Keep adding the cheapest edge that does not form a cycle until all vertices are joined. The greedy choice is safe for MST.",
      analogy: {
        icon: "treeIcon",
        label: "Cheapest wiring",
        text: "Wire up all houses with the least cable: repeatedly lay the cheapest new wire that connects a not-yet-connected house, never forming a loop.",
        breaks: "Greedy is optimal for MST specifically; it is not optimal for shortest paths or the travelling salesman.",
      },
      steps: [
        "A spanning tree has all $V$ vertices, $V-1$ edges, no cycle.",
        "Kruskal: sort edges by weight, add the next cheapest that joins two different components.",
        "Prim: grow one tree, repeatedly adding the cheapest edge leaving it.",
        "Use union-find (Kruskal) or a priority queue (Prim).",
        "Both yield a minimum-weight spanning tree.",
      ],
      example: "On a weighted graph, Kruskal picks edges in increasing weight, skipping any that would close a cycle, until $V-1$ edges are chosen. The result is the MST.",
      misconception: "An MST minimises total edge weight, not the path between two particular vertices. For point-to-point shortest paths use Dijkstra, not the MST.",
      rule: "Spanning tree: $V-1$ edges, no cycle. MST = cheapest one; Kruskal (sort + union-find) or Prim (grow + priority queue).",
    },
    {
      id: "shortest-paths",
      title: "Shortest Paths: Dijkstra and Bellman-Ford",
      explainer: null,
      what: "Single-source shortest paths: Dijkstra for non-negative weights, Bellman-Ford when negative edges are allowed.",
      why: "Routing, navigation and scheduling reduce to shortest paths. The right algorithm depends on whether edge weights can be negative.",
      intuition: "Dijkstra greedily finalises the closest unfinished vertex each step. Bellman-Ford relaxes every edge $V-1$ times, which tolerates negatives.",
      analogy: {
        icon: "map",
        label: "Cheapest route",
        text: "Dijkstra expands the nearest frontier first, like a GPS settling the closest intersections; Bellman-Ford keeps improving estimates over rounds.",
        breaks: "Dijkstra breaks with negative edges; Bellman-Ford handles them and even detects negative cycles.",
      },
      steps: [
        "Set the source distance to 0, all others to infinity.",
        "Dijkstra: repeatedly finalise the nearest unvisited vertex and relax its edges (needs non-negative weights).",
        "Dijkstra runs in $O((V+E)\\log V)$ with a heap.",
        "Bellman-Ford: relax all edges $V-1$ times; $O(VE)$.",
        "A further relaxation that still improves means a negative cycle exists.",
      ],
      example: "On a road network with positive distances, Dijkstra finds the shortest route from the start to every junction. If some edges give refunds (negative), use Bellman-Ford.",
      misconception: "Dijkstra is wrong on graphs with negative edge weights. Use Bellman-Ford there, which also flags negative-weight cycles.",
      rule: "Non-negative weights: Dijkstra $O((V+E)\\log V)$. Negative weights: Bellman-Ford $O(VE)$, detects negative cycles.",
    },
  ],
};
