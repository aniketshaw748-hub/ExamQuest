// Authored Explanation-Ladder lessons for Design & Analysis of Algorithms.
// Standard CS material (the verified PYQ answers still come from daa.json untouched).

export const TEACH_DAA = {
  ch1: [
    {
      id: "complexity-basics",
      title: "Time and Space Complexity",
      explainer: "complexity",
      what: "Algorithm analysis measures the time and space an algorithm needs as a function of input size $n$, independent of machine or language.",
      why: "You compare algorithms before coding them. A priori (theoretical) analysis predicts cost from the algorithm itself; a posteriori measures an actual run.",
      intuition: "Count the elementary operations (comparisons, assignments) as $n$ grows. The growth rate, not the exact count, is what scales.",
      analogy: {
        icon: "ruler",
        label: "Measure by growth",
        text: "Like rating cars by their fuel-economy curve, not one trip: you care how cost rises with distance ($n$), not a single number.",
        breaks: "Real runs vary with hardware; asymptotic cost abstracts that away.",
      },
      steps: [
        "Pick the input size $n$.",
        "Count elementary operations as a function of $n$.",
        "Time complexity = operation count; space complexity = memory cells used.",
        "Distinguish best, worst and average case.",
        "A priori = theoretical formula; a posteriori = measured run.",
      ],
      example: "Linear search does up to $n$ comparisons: worst case $n$, best case 1, average about $n/2$, so $\\Theta(n)$. Space is $S(X)=C(X)+I(X)$, fixed plus instance part.",
      misconception: "Time complexity is not seconds; it counts operations as a function of $n$. Faster hardware changes the constant, not the growth class.",
      rule: "Analyse by growth in $n$: count operations, separate best/worst/average, ignore machine constants.",
    },
    {
      id: "asymptotic-notation",
      title: "Asymptotic Notation",
      explainer: "bigo",
      what: "Asymptotic notation classifies growth: $O$ is an upper bound, $\\Omega$ a lower bound, $\\Theta$ a tight bound, with $o$ and $\\omega$ the strict versions.",
      why: "For large $n$ only the dominant term matters. Notation lets you say '$\\Theta(n\\log n)$' and compare algorithms without machine-specific detail.",
      intuition: "Drop constants and lower-order terms. $3n^2+5n+7$ is $\\Theta(n^2)$ because $n^2$ dwarfs the rest as $n$ grows.",
      analogy: {
        icon: "podium",
        label: "Who wins as n grows",
        text: "Each complexity class is a runner; as the track ($n$) lengthens the slower-growing function always wins, whatever head start (constant) the others had.",
        breaks: "For small $n$ a 'worse' class can be faster; asymptotics only describe the large-$n$ limit.",
      },
      steps: [
        "$f=O(g)$: $f \\le c\\,g$ for large $n$ (upper bound).",
        "$f=\\Omega(g)$: $f \\ge c\\,g$ (lower bound).",
        "$f=\\Theta(g)$: both hold, a tight bound.",
        "$o(g)$ is a loose upper bound ($f/g\\to 0$); $\\omega(g)$ a loose lower bound.",
        "Keep only the dominant term; drop constants.",
      ],
      example: "$O(\\log n) < O(n) < O(n\\log n) < O(n^2) < O(2^n)$. The lower bound for any comparison sort is $\\Omega(n\\log n)$, so merge sort at $\\Theta(n\\log n)$ is optimal.",
      misconception: "$O$ is only an upper bound, not 'exactly'. Calling an $O(n)$ algorithm '$O(n^2)$' is technically true but uselessly loose; use $\\Theta$ for a tight claim.",
      rule: "$O$ upper, $\\Omega$ lower, $\\Theta$ tight. Compare by dominant term; comparison sorts are $\\Omega(n\\log n)$.",
    },
    {
      id: "masters-theorem",
      title: "The Master Theorem",
      explainer: "master",
      what: "The Master Theorem solves divide-and-conquer recurrences $T(n)=aT(n/b)+f(n)$ by comparing $f(n)$ with $n^{\\log_b a}$.",
      why: "Most divide-and-conquer algorithms (merge sort, binary search, Strassen) give this recurrence. The theorem reads off the answer in one step instead of unrolling.",
      intuition: "Compare the split/combine work $f(n)$ against the leaf work $n^{\\log_b a}$. Whichever dominates sets the cost; if they tie, multiply by a log.",
      analogy: {
        icon: "ladder",
        label: "Top-heavy or bottom-heavy",
        text: "A recursion tree is heavy at the leaves ($n^{\\log_b a}$) or at the root ($f(n)$). The heavier end decides the total.",
        breaks: "It needs $f(n)$ to behave polynomially; some recurrences (like $f(n)=n/\\log n$) fall in the gaps and need other methods.",
      },
      steps: [
        "Write the recurrence as $T(n)=aT(n/b)+f(n)$.",
        "Compute the watershed $n^{\\log_b a}$.",
        "Case 1: $f(n)$ polynomially smaller $\\Rightarrow T=\\Theta(n^{\\log_b a})$.",
        "Case 2: $f(n)=\\Theta(n^{\\log_b a}\\log^k n) \\Rightarrow T=\\Theta(n^{\\log_b a}\\log^{k+1} n)$.",
        "Case 3: $f(n)$ polynomially larger and regular $\\Rightarrow T=\\Theta(f(n))$.",
      ],
      example: "Merge sort $T(n)=2T(n/2)+n$: here $n^{\\log_2 2}=n$ and $f(n)=n$, Case 2 with $k=0$, so $T(n)=\\Theta(n\\log n)$. Binary search $T(n)=T(n/2)+1=\\Theta(\\log n)$.",
      misconception: "The Master Theorem only fits $aT(n/b)+f(n)$ with constant $a,b$. Recurrences like $T(n)=T(n-1)+n$ are not divide-and-conquer; unroll those instead.",
      rule: "Compare $f(n)$ with $n^{\\log_b a}$: smaller, equal-times-log, or larger picks the case.",
    },
    {
      id: "recurrence-methods",
      title: "Recursion-Tree and Substitution",
      explainer: "rectree",
      what: "When the Master Theorem does not apply, solve recurrences by the recursion-tree method or the substitution (guess-and-induct) method.",
      why: "Many recurrences fall outside the Master form. These two methods handle the rest and build the intuition behind the Master Theorem.",
      intuition: "Recursion tree: draw the cost at each level and sum the series. Substitution: guess the bound, then prove it by induction.",
      analogy: {
        icon: "stack",
        label: "Sum the levels",
        text: "Unfold the recursion into a tree; each level is a row of work. Add the rows (often a geometric or arithmetic series) to get the total.",
        breaks: "Substitution needs a correct guess; a wrong guess can seem to work until the induction fails on the constants.",
      },
      steps: [
        "Recursion tree: expand the recurrence level by level.",
        "Find the per-level cost and the number of levels.",
        "Sum the series across all levels.",
        "Substitution: guess $T(n)=O(g(n))$.",
        "Prove the guess by induction, fixing the constant.",
      ],
      example: "$T(n)=2T(n/2)+n$ has a recursion tree of $\\log n$ levels each costing $n$, total $\\Theta(n\\log n)$. Substitution then verifies $T(n)\\le cn\\log n$ by induction.",
      misconception: "A recursion tree gives a guess, not always a proof; confirm it with substitution. And the per-level cost can change between levels, do not assume it is constant.",
      rule: "Recursion tree to sum level costs; substitution to guess then prove by induction.",
    },
  ],
  ch2: [
    {
      id: "greedy",
      title: "Greedy Algorithms",
      explainer: "greedy",
      what: "A greedy algorithm builds a solution by repeatedly taking the choice that looks best right now, and never undoing it.",
      why: "When a problem has the greedy-choice property and optimal substructure, this fast, simple strategy is provably optimal (coin change, Huffman, MST, activity selection).",
      intuition: "Prioritise the options, then grab the best available one each step and commit. No reconsidering, no backtracking.",
      analogy: {
        icon: "coins",
        label: "Make change fast",
        text: "To make change, keep handing over the largest coin that still fits. For standard coins that gives the fewest coins.",
        breaks: "Greedy is optimal only when the local best forces the global best; for coins like 1, 3, 4 it fails and you need dynamic programming.",
      },
      steps: [
        "Define the greedy choice (largest, earliest-finishing, cheapest edge...).",
        "Repeatedly take that choice if it stays feasible.",
        "Never reconsider a past choice.",
        "Prove the greedy-choice property to know it is optimal.",
        "Examples: Huffman coding, Kruskal/Prim MST, activity selection.",
      ],
      example: "Activity selection: repeatedly pick the next activity that finishes earliest among those compatible. This maximises the number of activities.",
      misconception: "Greedy is not always optimal. It works only for problems with the greedy-choice property; otherwise it can be far from best.",
      rule: "Greedy: take the locally best choice and commit. Optimal only with the greedy-choice property + optimal substructure.",
    },
    {
      id: "divide-conquer",
      title: "Divide and Conquer",
      explainer: "rectree",
      what: "Divide and conquer splits a problem into smaller subproblems of the same type, solves them recursively, and combines the results.",
      why: "It turns many problems into clean recurrences with fast solutions: merge sort, quicksort, binary search, Strassen, FFT.",
      intuition: "Break the input into pieces (usually halves), recurse on each, then merge. The cost is captured by $T(n)=aT(n/b)+f(n)$.",
      analogy: {
        icon: "stack",
        label: "Split, solve, merge",
        text: "Sorting a deck of cards: split it in two, sort each half, then merge the two sorted halves into one.",
        breaks: "It pays off only when subproblems are independent and combining is cheaper than solving from scratch.",
      },
      steps: [
        "Divide the input into subproblems (often two halves).",
        "Conquer each one recursively.",
        "Combine the sub-results into the final answer.",
        "Cost follows $T(n) = aT(n/b) + f(n)$.",
        "Solve the recurrence with the Master theorem or a recursion tree.",
      ],
      example: "Merge sort splits in half, sorts each, and merges in linear time: $T(n)=2T(n/2)+n=\\Theta(n\\log n)$.",
      misconception: "More subproblems is not automatically faster; the combine step $f(n)$ can dominate. Always analyse the recurrence.",
      rule: "Divide and conquer = split + recurse + combine; cost $T(n)=aT(n/b)+f(n)$.",
    },
    {
      id: "dynamic-programming",
      title: "Dynamic Programming",
      explainer: "dptable",
      what: "Dynamic programming solves a problem by combining answers to overlapping subproblems, storing each subproblem's answer exactly once.",
      why: "When subproblems repeat (Fibonacci, knapsack, LCS, shortest paths), recomputing them is exponential; storing them makes it polynomial.",
      intuition: "Find the recurrence relating an answer to smaller answers, then fill a table bottom-up (or memoise top-down) so each subproblem is solved a single time.",
      analogy: {
        icon: "grid",
        label: "Fill the table once",
        text: "Build a table whose every cell uses already-filled cells, like a spreadsheet whose formulas reference earlier columns.",
        breaks: "DP needs optimal substructure AND overlapping subproblems; with no overlap, plain divide and conquer is enough.",
      },
      steps: [
        "Identify the subproblems and a recurrence between them.",
        "Order them so each depends only on smaller ones.",
        "Fill a table bottom-up (or memoise the recursion).",
        "Each subproblem is computed exactly once.",
        "Read the final answer off the table.",
      ],
      example: "Fibonacci $F(i)=F(i-1)+F(i-2)$ filled in a table is $\\Theta(n)$ versus exponential naive recursion. 0/1 knapsack fills an items-by-capacity grid.",
      misconception: "DP is not just recursion; the win is REUSE. Without storing results, the same subproblems are recomputed exponentially.",
      rule: "DP = recurrence + store each overlapping subproblem once (optimal substructure).",
    },
    {
      id: "backtracking",
      title: "Backtracking",
      explainer: "rectree",
      what: "Backtracking explores a tree of partial solutions, extending one choice at a time and abandoning (pruning) a branch as soon as it cannot lead to a valid solution.",
      why: "For constraint problems with a huge search space (N-queens, Sudoku, subset-sum, graph colouring), pruning dead branches early makes brute force feasible.",
      intuition: "Build the solution incrementally; at each step try an option, recurse, and if it fails undo it (backtrack) and try the next.",
      analogy: {
        icon: "ladder",
        label: "Explore and retreat",
        text: "Walking a maze: follow a path until it dead-ends, then back up to the last junction and try another route.",
        breaks: "Pruning is what saves it; without good bounds backtracking degenerates into full brute force.",
      },
      steps: [
        "Extend the current partial solution by one choice.",
        "Check the constraints; if violated, prune this branch.",
        "Recurse to extend further.",
        "If stuck, undo the last choice and try the next option.",
        "Report solutions found at complete leaves.",
      ],
      example: "N-queens: place a queen column by column and backtrack whenever a placement is attacked, pruning whole subtrees.",
      misconception: "Backtracking is brute force WITH pruning, not blind enumeration. Rejecting partial solutions early is the whole point.",
      rule: "Backtracking: build incrementally, prune invalid partial solutions, undo and retry on failure.",
    },
  ],
  ch3: [
    {
      id: "graph-representations",
      title: "Representing Graphs",
      explainer: "gmatrix",
      what: "A graph is stored as an adjacency matrix ($V\\times V$ of 0/1) or an adjacency list (each vertex lists its neighbours).",
      why: "The representation sets the cost of every later operation. Matrices give $O(1)$ edge lookups but $O(V^2)$ space; lists are compact for sparse graphs.",
      intuition: "Matrix = a full grid of who-connects-to-whom. List = each vertex carries a small list of its neighbours, cheap when edges are few.",
      analogy: {
        icon: "grid",
        label: "Grid vs contact list",
        text: "Adjacency matrix is a seating grid marking every pair; adjacency list is each person's contacts, only the people they actually know.",
        breaks: "Matrices waste space on sparse graphs; lists make 'is there an edge u-v?' slower ($O(\\deg)$ instead of $O(1)$).",
      },
      steps: [
        "Adjacency matrix: $A_{ij}=1$ if edge $ij$ exists.",
        "Symmetric for undirected graphs; space $O(V^2)$.",
        "Adjacency list: each vertex stores its neighbours.",
        "Space $O(V+E)$, great for sparse graphs.",
        "Choose by density: dense $\\Rightarrow$ matrix, sparse $\\Rightarrow$ list.",
      ],
      example: "A graph with 1000 vertices and 2000 edges wastes a million matrix cells but needs only about 3000 list entries. Edge query is $O(1)$ in a matrix, $O(\\deg v)$ in a list.",
      misconception: "Adjacency-matrix space is $O(V^2)$ regardless of edge count, so it is wasteful for sparse graphs. Match the representation to graph density.",
      rule: "Matrix: $O(V^2)$ space, $O(1)$ edge test. List: $O(V+E)$ space, best for sparse graphs.",
    },
    {
      id: "bfs-dfs",
      title: "Breadth-First and Depth-First Search",
      explainer: "graph",
      what: "BFS explores a graph level by level using a queue; DFS goes as deep as possible first using a stack or recursion.",
      why: "These are the backbone of graph algorithms: BFS finds shortest paths in unweighted graphs; DFS detects cycles, finds connected components and orders for topological sort.",
      intuition: "BFS ripples outward like a wave from the source. DFS dives down one path until stuck, then backtracks.",
      analogy: {
        icon: "map",
        label: "Flood vs explore",
        text: "BFS floods the nearest rooms first (a wave); DFS sends one explorer down a corridor to the end, then backtracks to the last junction.",
        breaks: "BFS shortest paths only hold for unweighted edges; with weights you need Dijkstra.",
      },
      steps: [
        "BFS: from the source, visit all neighbours, then their neighbours, using a queue.",
        "BFS gives shortest path counts in unweighted graphs.",
        "DFS: follow one edge as deep as possible, backtrack at dead ends, using a stack or recursion.",
        "Both run in $O(V+E)$ with adjacency lists.",
        "Mark visited vertices so you never revisit.",
      ],
      example: "On an unweighted graph, BFS from $s$ labels every vertex with its hop-distance from $s$. DFS on the same graph reveals back edges, which signal cycles.",
      misconception: "BFS and DFS visit the same vertices but in different orders; only BFS gives shortest paths (in unweighted graphs). DFS order is not by distance.",
      rule: "BFS = queue, level order, unweighted shortest paths. DFS = stack/recursion, deep first, cycles and components. Both $O(V+E)$.",
    },
    {
      id: "spanning-trees-mst",
      title: "Spanning Trees and MST",
      explainer: "tree",
      what: "A spanning tree connects all $V$ vertices with $V-1$ edges and no cycle. A minimum spanning tree (MST) is the cheapest such tree by total edge weight.",
      why: "MST answers 'connect everything as cheaply as possible' (networks, roads). Kruskal and Prim both build it greedily and provably optimally.",
      intuition: "Keep adding the cheapest edge that does not form a cycle until all vertices are joined. The greedy choice is safe for MST.",
      analogy: {
        icon: "treeIcon",
        label: "Cheapest wiring",
        text: "Wire up all houses with the least cable: repeatedly lay the cheapest new wire that connects a not-yet-connected house, never forming a loop.",
        breaks: "Greedy is optimal for MST specifically; it is not optimal for shortest paths or the travelling salesman.",
      },
      steps: [
        "A spanning tree has all $V$ vertices, $V-1$ edges, no cycle.",
        "Kruskal: sort edges by weight, add the next cheapest that joins two different components.",
        "Prim: grow one tree, repeatedly adding the cheapest edge leaving it.",
        "Use union-find (Kruskal) or a priority queue (Prim).",
        "Both yield a minimum-weight spanning tree.",
      ],
      example: "On a weighted graph, Kruskal picks edges in increasing weight, skipping any that would close a cycle, until $V-1$ edges are chosen. The result is the MST.",
      misconception: "An MST minimises total edge weight, not the path between two particular vertices. For point-to-point shortest paths use Dijkstra, not the MST.",
      rule: "Spanning tree: $V-1$ edges, no cycle. MST = cheapest one; Kruskal (sort + union-find) or Prim (grow + priority queue).",
    },
    {
      id: "shortest-paths",
      title: "Shortest Paths: Dijkstra and Bellman-Ford",
      explainer: "dijkstra",
      what: "Single-source shortest paths: Dijkstra for non-negative weights, Bellman-Ford when negative edges are allowed.",
      why: "Routing, navigation and scheduling reduce to shortest paths. The right algorithm depends on whether edge weights can be negative.",
      intuition: "Dijkstra greedily finalises the closest unfinished vertex each step. Bellman-Ford relaxes every edge $V-1$ times, which tolerates negatives.",
      analogy: {
        icon: "map",
        label: "Cheapest route",
        text: "Dijkstra expands the nearest frontier first, like a GPS settling the closest intersections; Bellman-Ford keeps improving estimates over rounds.",
        breaks: "Dijkstra breaks with negative edges; Bellman-Ford handles them and even detects negative cycles.",
      },
      steps: [
        "Set the source distance to 0, all others to infinity.",
        "Dijkstra: repeatedly finalise the nearest unvisited vertex and relax its edges (needs non-negative weights).",
        "Dijkstra runs in $O((V+E)\\log V)$ with a heap.",
        "Bellman-Ford: relax all edges $V-1$ times; $O(VE)$.",
        "A further relaxation that still improves means a negative cycle exists.",
      ],
      example: "On a road network with positive distances, Dijkstra finds the shortest route from the start to every junction. If some edges give refunds (negative), use Bellman-Ford.",
      misconception: "Dijkstra is wrong on graphs with negative edge weights. Use Bellman-Ford there, which also flags negative-weight cycles.",
      rule: "Non-negative weights: Dijkstra $O((V+E)\\log V)$. Negative weights: Bellman-Ford $O(VE)$, detects negative cycles.",
    },
    {
      id: "floyd-warshall",
      title: "Floyd-Warshall: All-Pairs Shortest Paths",
      explainer: "gmatrix",
      what: "Floyd-Warshall finds the shortest path between EVERY pair of vertices by dynamic programming over an intermediate-vertex index $k$.",
      why: "All-pairs shortest paths is a guaranteed Group-C algorithm; its compact triple loop and $O(V^3)$ cost are classic exam material.",
      intuition: "Allow paths to use vertices $1..k$ as intermediates, adding one more $k$ each round; each step just asks 'is going through $k$ shorter than what I had?'",
      analogy: {
        icon: "grid",
        label: "Unlocking transfer hubs",
        text: "Progressively unlock which cities you may change trains at; as each new hub opens, update the best route between every pair.",
        breaks: "It tolerates negative edges but breaks on a negative cycle (distances would spiral to minus infinity).",
      },
      steps: [
        "Start with the weight matrix $D$ (direct edges, $\\infty$ where there is none, 0 on the diagonal).",
        "For $k=1..n$: for all $i,j$: $D[i][j]=\\min(D[i][j],\\ D[i][k]+D[k][j])$.",
        "After all $k$, $D[i][j]$ is the shortest distance for every pair.",
        "Time $O(V^3)$, space $O(V^2)$.",
        "The same loops with OR/AND instead of min/+ give the transitive closure (reachability).",
      ],
      example: "On a dense graph, one triple loop fills the entire distance matrix, simpler to code and often faster than running Dijkstra from every source.",
      misconception: "The $k$ loop must be OUTERMOST; swapping it with the $i$ or $j$ loop gives wrong answers. Floyd allows negative edges but not negative cycles.",
      rule: "Floyd-Warshall: $D[i][j]=\\min(D[i][j],\\ D[i][k]+D[k][j])$ with $k$ outermost; $O(V^3)$ all-pairs.",
    },
    {
      id: "network-flow",
      title: "Network Flow and Max-Flow Min-Cut",
      explainer: "graph",
      what: "A flow network is a directed graph with edge capacities, a source $s$ and a sink $t$. Ford-Fulkerson repeatedly pushes flow along augmenting paths in the residual graph until none remain; the maximum flow equals the minimum cut.",
      why: "Max-flow (find the value, show each step) appears almost every year, and the max-flow min-cut theorem is a headline result.",
      intuition: "Keep finding an $s$-to-$t$ path with spare capacity and push as much as its tightest edge allows; when no such path is left you have the maximum, which is exactly the cheapest cut separating $s$ from $t$.",
      analogy: {
        icon: "map",
        label: "Saturating the pipes",
        text: "Water from a source to a sink: push more through any route with slack until every route is saturated. The bottleneck set of pipes is the minimum cut.",
        breaks: "Choosing augmenting paths badly can be slow on integer capacities; picking shortest paths (Edmonds-Karp) keeps it polynomial.",
      },
      steps: [
        "Capacities $c(u,v)\\ge 0$, a source $s$ and a sink $t$; start all flow at 0.",
        "Find an augmenting path: an $s\\to t$ path in the RESIDUAL graph with every residual capacity $> 0$.",
        "Push the bottleneck (the minimum residual capacity) along it; update residual capacities and back-edges.",
        "Repeat until no augmenting path remains.",
        "Max flow $=$ sum of bottlenecks $=$ capacity of the minimum cut (the max-flow min-cut theorem).",
      ],
      example: "Each augmenting path adds its bottleneck to the running total; once the residual graph has no $s\\to t$ path, the flow is maximum and equals the capacity of the minimum cut.",
      misconception: "You must search the RESIDUAL graph (with back-edges), not the original; back-edges let the algorithm reroute earlier flow. Max flow equals the min CUT capacity, not the number of paths.",
      rule: "Ford-Fulkerson augments along residual $s\\to t$ paths by the bottleneck; max flow = min cut.",
    },
  ],
  ch4: [
    {
      id: "p-np",
      title: "P, NP and Verification",
      explainer: "npclasses",
      what: "P is the class of problems solvable in polynomial time; NP is the class where a proposed solution is verifiable in polynomial time. Every problem in P is in NP.",
      why: "This split organises problems by difficulty and frames the biggest open question in computer science: does $P = NP$?",
      intuition: "Solving from scratch (P) is harder than checking a given answer (NP). A Sudoku is hard to solve but easy to check.",
      analogy: {
        icon: "checklist",
        label: "Solve vs check",
        text: "Finding a solution can be slow, but verifying one someone hands you is fast. That gap is the heart of P versus NP.",
        breaks: "For problems in P, solving is itself fast, so the solve/check gap vanishes.",
      },
      steps: [
        "P: a polynomial-time algorithm solves it outright.",
        "NP: given a candidate certificate, a polynomial-time checker verifies it.",
        "Every problem in P is in NP (the solution is its own certificate).",
        "Whether $P = NP$ is unknown.",
        "Most researchers believe $P \\ne NP$.",
      ],
      example: "Sorting is in P. Checking a given Hamiltonian cycle is easy (NP), but finding one seems hard.",
      misconception: "NP does not mean 'not polynomial'. It means 'verifiable in polynomial time' (nondeterministic polynomial), and $P \\subseteq NP$.",
      rule: "P = solvable fast; NP = checkable fast. $P \\subseteq NP$; whether they are equal is open.",
    },
    {
      id: "np-complete",
      title: "NP-Complete and NP-Hard",
      explainer: "npclasses",
      what: "NP-complete problems are the hardest in NP: in NP, and every NP problem reduces to them. NP-hard problems are at least that hard but need not be in NP.",
      why: "If any NP-complete problem had a polynomial algorithm, all of NP would, settling $P = NP$. They mark the boundary of feasible computation.",
      intuition: "An NP-complete problem is a universal NP problem: solve it fast and you solve everything in NP fast.",
      analogy: {
        icon: "clubs",
        label: "The hardest in the club",
        text: "NP-complete problems are the toughest members of NP; cracking one cracks them all, because everything in NP reduces to it.",
        breaks: "NP-hard reaches beyond NP, for example the halting problem is NP-hard but not even in NP.",
      },
      steps: [
        "NP-complete: in NP, and every NP problem reduces to it.",
        "Cook-Levin: SAT was the first proven NP-complete problem.",
        "Show new problems NP-complete by reducing a known one to them.",
        "NP-hard: at least as hard as NP-complete, possibly not in NP.",
        "A poly algorithm for one NP-complete problem gives $P = NP$.",
      ],
      example: "SAT, 3-colouring, vertex cover, Hamiltonian cycle and decision-TSP are NP-complete. TSP optimisation and the halting problem are NP-hard.",
      misconception: "NP-hard is not a subset of NP. NP-complete is exactly $NP \\cap \\text{NP-hard}$, the hardest problems still in NP.",
      rule: "NP-complete = in NP and every NP problem reduces to it; NP-hard = at least that hard, maybe outside NP.",
    },
    {
      id: "reductions",
      title: "Polynomial Reductions",
      explainer: "reduction",
      what: "A polynomial reduction $A \\le_p B$ transforms instances of $A$ into instances of $B$ in polynomial time, so a fast solver for $B$ yields a fast solver for $A$.",
      why: "Reductions are how hardness is proved and transferred: to show $B$ is NP-hard, reduce a known NP-hard problem $A$ to $B$.",
      intuition: "If you can rephrase every $A$-question as a $B$-question cheaply, then $B$ is at least as hard as $A$.",
      analogy: {
        icon: "map",
        label: "Translate the problem",
        text: "Convert your problem into one you already have a solver for; if that solver is fast, so is yours.",
        breaks: "The transformation itself must be polynomial; an expensive transformation proves nothing.",
      },
      steps: [
        "Map each instance of $A$ to an instance of $B$ in polynomial time.",
        "Yes-instances must map to yes-instances, and no to no.",
        "Then a $B$-solver solves $A$: transform, solve, return the answer.",
        "Contrapositive: if $A$ is hard, $B$ is hard.",
        "To prove $B$ NP-hard, reduce a known NP-hard $A$ to $B$.",
      ],
      example: "3-SAT reduces to vertex cover, which proves vertex cover NP-hard. Most NP-completeness proofs chain reductions back to SAT.",
      misconception: "The direction matters: reduce the KNOWN-hard problem TO the new one to prove the new one hard, not the reverse.",
      rule: "$A \\le_p B$: poly-transform $A$ into $B$; $B$ easy implies $A$ easy, so $A$ hard implies $B$ hard.",
    },
  ],
  ch5: [
    {
      id: "approximation",
      title: "Approximation Algorithms",
      explainer: "approxratio",
      what: "An approximation algorithm runs in polynomial time and returns a solution provably within a factor $\\rho$ of the optimum, for an NP-hard optimization problem.",
      why: "When exact optimisation is NP-hard, a fast answer guaranteed close to optimal is often good enough, and far better than no answer.",
      intuition: "Give up on the exact optimum (too expensive) and instead guarantee 'never worse than $\\rho$ times optimal'.",
      analogy: {
        icon: "balance",
        label: "Close enough, fast",
        text: "Like a quote guaranteed within 20% of the best price: not perfect, but fast and bounded.",
        breaks: "The ratio $\\rho$ is a worst-case bound; on many inputs the algorithm does much better.",
      },
      steps: [
        "The exact problem is NP-hard, so accept a near-optimal answer.",
        "Design a poly-time algorithm with a proven ratio $\\rho$.",
        "Minimisation: $\\text{cost} \\le \\rho\\cdot\\text{OPT}$; maximisation: $\\text{value} \\ge \\text{OPT}/\\rho$.",
        "Smaller $\\rho$ (closer to 1) is a better guarantee.",
        "Some problems admit a PTAS (any ratio $1+\\varepsilon$); others resist approximation.",
      ],
      example: "Vertex cover has a 2-approximation; metric TSP has a 1.5-approximation (Christofides).",
      misconception: "A ratio is a worst-case guarantee, not the typical error. And not every NP-hard problem is approximable, some are hard even to approximate.",
      rule: "Approximation: poly-time, solution within factor $\\rho$ of optimal ($\\rho \\to 1$ is better).",
    },
    {
      id: "vertex-cover",
      title: "Vertex Cover and the 2-Approximation",
      explainer: "approxratio",
      what: "A vertex cover is a set of vertices touching every edge. Finding the smallest is NP-hard, but a greedy edge-picking algorithm gives a 2-approximation.",
      why: "It is the textbook example of turning an NP-hard optimisation into a fast algorithm with a provable guarantee.",
      intuition: "Repeatedly take an uncovered edge and add BOTH endpoints. Each chosen edge needs at least one endpoint in any cover, so you use at most twice the optimum.",
      analogy: {
        icon: "handshake",
        label: "Grab both ends",
        text: "For each uncovered connection, recruit both people. The optimum needs at least one of each pair, so you overshoot by at most a factor of two.",
        breaks: "Picking both endpoints is exactly what bounds the ratio at 2; it is in fact hard to approximate much below 2.",
      },
      steps: [
        "While an uncovered edge remains, pick it.",
        "Add both its endpoints to the cover.",
        "Remove every edge now covered.",
        "The picked edges share no vertex, so $\\text{OPT} \\ge$ their count.",
        "The cover is $2\\times$ that count, so $\\text{cost} \\le 2\\cdot\\text{OPT}$.",
      ],
      example: "On a path of 3 edges the greedy cover takes at most 4 vertices while the optimum is 2, within the $2\\times$ guarantee.",
      misconception: "Adding both endpoints looks wasteful, but it is what makes the ratio provably 2; adding just one loses the guarantee.",
      rule: "Vertex cover: take both endpoints of disjoint uncovered edges, a 2-approximation.",
    },
    {
      id: "tsp-approx",
      title: "Approximating TSP",
      explainer: "approxratio",
      what: "The travelling salesman problem (visit every city once, minimise distance) is NP-hard. Under the triangle inequality, an MST-doubling tour is a 2-approximation and Christofides gives 1.5.",
      why: "TSP is the canonical hard optimisation; its metric version shows how structure (the triangle inequality) enables good approximations.",
      intuition: "Build a minimum spanning tree, walk around it doubling each edge, and shortcut past repeated cities. Shortcutting only helps, so the tour is at most twice the MST, hence at most twice optimal.",
      analogy: {
        icon: "map",
        label: "Walk the tree, take shortcuts",
        text: "Trace the cheapest network (MST) and skip cities already seen; the detour you save keeps the trip short.",
        breaks: "These guarantees need the triangle inequality; general (non-metric) TSP has no constant-factor approximation unless $P = NP$.",
      },
      steps: [
        "Build a minimum spanning tree of the cities.",
        "Double its edges to get a walk visiting every city.",
        "Shortcut past already-visited cities (the triangle inequality keeps it shorter).",
        "The result is at most $2\\cdot\\text{MST} \\le 2\\cdot\\text{OPT}$.",
        "Christofides improves the doubling to a 1.5-approximation.",
      ],
      example: "On metric points, the MST-doubling tour is within $2\\times$ the optimal tour; Christofides tightens this to $1.5\\times$.",
      misconception: "These ratios hold ONLY under the triangle inequality (metric TSP). General TSP is inapproximable unless $P = NP$.",
      rule: "Metric TSP: MST-doubling is a 2-approximation, Christofides is 1.5. Needs the triangle inequality.",
    },
  ],
  ch6: [
    {
      id: "strassen",
      title: "Strassen's Matrix Multiplication",
      explainer: "master",
      what: "Strassen multiplies two $n\\times n$ matrices with 7 recursive products of $n/2$ sub-matrices instead of the naive 8, giving $T(n)=7T(n/2)+\\Theta(n^2)=\\Theta(n^{2.81})$.",
      why: "It was the first algorithm to beat the obvious $O(n^3)$ matrix multiplication, a landmark divide-and-conquer result.",
      intuition: "Split each matrix into four quadrants. The schoolbook method needs 8 quadrant products; Strassen combines additions cleverly to need only 7, and fewer multiplications win as $n$ grows.",
      analogy: {
        icon: "grid",
        label: "Trade multiplies for adds",
        text: "Like a multiplication trick that replaces a costly multiply with several cheap additions; over a deep recursion those saved multiplies compound.",
        breaks: "Large constant factors and extra additions make Strassen pay off only for big matrices.",
      },
      steps: [
        "Split each $n\\times n$ matrix into four $n/2$ quadrants.",
        "Naive needs 8 quadrant multiplications: $O(n^3)$.",
        "Strassen forms 7 products via clever sums and differences.",
        "Recurrence $T(n)=7T(n/2)+\\Theta(n^2)$.",
        "Master theorem: $T(n)=\\Theta(n^{\\log_2 7})\\approx\\Theta(n^{2.81})$.",
      ],
      example: "In the explainer above set $a=7,\\ b=2,\\ f=n^2$: $\\log_2 7 \\approx 2.81 > 2$, so Case 1 gives $\\Theta(n^{2.81})$, better than $n^3$.",
      misconception: "Strassen is asymptotically faster but has large constants and worse numerical stability; libraries use it only for big matrices.",
      rule: "Strassen: 7 multiplications per level, $T(n)=7T(n/2)+\\Theta(n^2)=\\Theta(n^{2.81})$.",
    },
    {
      id: "branch-bound",
      title: "Branch and Bound",
      explainer: "rectree",
      what: "Branch and bound searches the tree of partial solutions to an optimisation problem, using a bound on the best achievable value to prune subtrees that cannot beat the current best.",
      why: "For NP-hard optimisation (knapsack, TSP, the 15-puzzle) it finds the exact optimum far faster than brute force by cutting off hopeless branches.",
      intuition: "Like backtracking, but each node carries a bound (an optimistic estimate). If a node's bound is worse than a solution already found, prune the whole subtree.",
      analogy: {
        icon: "ladder",
        label: "Prune the hopeless",
        text: "Exploring routes, you abandon any partial route whose best-case cost already exceeds the best full route found so far.",
        breaks: "Pruning power depends on a good bound; a weak bound degenerates into exhaustive search.",
      },
      steps: [
        "Branch: split the problem into subproblems (a search tree).",
        "Bound: compute an optimistic estimate of each node's best solution.",
        "Keep the best complete solution found so far (the incumbent).",
        "Prune any node whose bound cannot beat the incumbent.",
        "Explore the rest; the surviving best is optimal.",
      ],
      example: "0/1 knapsack: bound a node by the fractional (greedy) value of the remaining items, then prune nodes whose bound is below the best packing found.",
      misconception: "Branch and bound finds the EXACT optimum (unlike greedy or approximation); the bound only prunes, it does not approximate.",
      rule: "Branch and bound = search tree + optimistic bound to prune subtrees that cannot beat the best so far.",
    },
    {
      id: "fft",
      title: "The Fast Fourier Transform",
      explainer: "master",
      what: "The FFT computes the discrete Fourier transform of $n$ points in $\\Theta(n\\log n)$ instead of $\\Theta(n^2)$, by divide and conquer on even- and odd-indexed samples.",
      why: "It makes polynomial multiplication and signal processing fast: multiplying degree-$n$ polynomials drops from $O(n^2)$ to $O(n\\log n)$.",
      intuition: "Split the sequence into even and odd indices, transform each half recursively, then combine with roots of unity. $T(n)=2T(n/2)+\\Theta(n)$.",
      analogy: {
        icon: "stack",
        label: "Split even and odd",
        text: "Halve the problem by separating even and odd samples, solve each half, then stitch them together cheaply, the merge-sort shape applied to frequencies.",
        breaks: "It relies on the special structure of roots of unity; arbitrary transforms do not factor this way.",
      },
      steps: [
        "Goal: evaluate a polynomial at the $n$-th roots of unity.",
        "Split into even-indexed and odd-indexed coefficients.",
        "Recursively FFT each half (size $n/2$).",
        "Combine with the roots of unity (the butterfly step).",
        "$T(n)=2T(n/2)+\\Theta(n)=\\Theta(n\\log n)$.",
      ],
      example: "In the explainer above set $a=2,\\ b=2,\\ f=n$: Case 2 gives $\\Theta(n\\log n)$, versus $\\Theta(n^2)$ for the direct DFT.",
      misconception: "FFT is the same transform as the DFT, computed faster. It does not change the result, only the running time.",
      rule: "FFT: divide on even/odd indices, $T(n)=2T(n/2)+\\Theta(n)=\\Theta(n\\log n)$.",
    },
  ],
};
