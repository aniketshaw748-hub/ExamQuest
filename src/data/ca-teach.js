// Authored Explanation-Ladder lessons for Computer Architecture.
// Standard architecture material (the verified PYQ answers come from ca.json untouched).

export const TEACH_CA = {
  ch1: [
    {
      id: "pipeline-basics",
      title: "Pipelining: The Assembly Line",
      explainer: "pipeline",
      what: "A pipeline splits instruction execution into $k$ stages so that, like an assembly line, several instructions are in different stages at the same time.",
      why: "Without overlap a processor finishes one instruction before starting the next. Pipelining overlaps them to raise throughput, the central idea of modern CPUs.",
      intuition: "Stage 1 works on instruction 3 while stage 2 works on instruction 2 and stage 3 on instruction 1. Once full, one instruction completes every cycle.",
      analogy: {
        icon: "gear",
        label: "Car assembly line",
        text: "A car factory does not build one car start to finish before the next; each station works on a different car at once, so a finished car rolls off every few minutes.",
        breaks: "The line stalls if a station is slow or a car needs a part not ready yet, just as pipelines stall on hazards.",
      },
      steps: [
        "Split execution into $k$ stages (fetch, decode, execute, ...).",
        "Each stage holds a different instruction per cycle.",
        "After a $k-1$ cycle fill, the pipeline produces one result per cycle.",
        "Each stage = combinational logic (does the work) + a sequential latch (holds the result); the clock period $=$ slowest stage delay $+$ latch delay, so stages should be balanced.",
        "Hazards can force the pipeline to stall.",
      ],
      example: "The classic 5-stage instruction pipeline is IF, ID, EX, MEM, WB (fetch, decode, execute, memory, write-back). A 4-stage pipeline overlaps 4 instructions; once full, instruction $i$ finishes while $i{+}1,i{+}2,i{+}3$ are still in earlier stages.",
      misconception: "Pipelining improves throughput (instructions per second), not the latency of any single instruction, which still passes through all $k$ stages. For MCQs: a stage holds BOTH combinational logic and a latch, and pipelining exploits TEMPORAL parallelism.",
      rule: "Pipeline = $k$ overlapped stages; after fill, one instruction completes per cycle.",
    },
    {
      id: "speedup-throughput",
      title: "Speedup, Efficiency and Throughput",
      explainer: "pipeline",
      what: "A $k$-stage pipeline completes $n$ tasks in $k + (n-1)$ cycles, giving speedup $\\dfrac{nk}{k+n-1}$, which approaches $k$ for large $n$.",
      why: "These formulas are the most common pipeline PYQs. They quantify how much overlap actually buys you and why it never quite reaches a perfect $k\\times$.",
      intuition: "The first task takes $k$ cycles to fill the pipe; after that each extra task adds just one cycle. So $n$ tasks cost $k + (n-1)$, not $nk$.",
      analogy: {
        icon: "clock",
        label: "Fill then flow",
        text: "Filling the assembly line costs $k-1$ cycles of warm-up; after that a product leaves every cycle, so long runs amortise the warm-up away.",
        breaks: "The ideal $k\\times$ speedup assumes no stalls and perfectly balanced stages; real hazards lower it.",
      },
      steps: [
        "Non-pipelined time for $n$ tasks: $nk$ cycles.",
        "Pipelined time: $k + (n-1)$ cycles.",
        "Speedup $S = \\dfrac{nk}{k+n-1}$.",
        "Efficiency $= S/k = \\dfrac{n}{k+n-1}$.",
        "Throughput $= \\dfrac{n}{k+n-1}$ tasks per cycle; both $\\to 1$ and $S\\to k$ as $n\\to\\infty$.",
      ],
      example: "The standard PYQ: a 4-stage pipeline, $n=64$ tasks. $S = \\dfrac{64\\cdot 4}{4+63} = \\dfrac{256}{67} \\approx 3.82$, close to the ceiling of 4. For just $n=4$: $S = \\dfrac{16}{7}\\approx 2.3$. When stage and latch delays are given, clock period $=$ (max stage delay $+$ latch delay), and speedup $=$ (non-pipelined time per instruction) $/$ (pipeline clock period).",
      misconception: "Speedup is not simply $k$; it is $\\dfrac{nk}{k+n-1}$ and only approaches the maximum $k$ for large $n$. A common error is using $nk$ as the pipelined time, that is the NON-pipelined time; the pipelined time is $k+n-1$.",
      rule: "$n$ tasks, $k$ stages: $k+n-1$ cycles, speedup $S=\\dfrac{nk}{k+n-1}$, max speedup $=k$, efficiency $=S/k$, throughput $=\\dfrac{n}{(k+n-1)t}$.",
    },
    {
      id: "pipeline-hazards",
      title: "Pipeline Hazards",
      explainer: "pipeline",
      what: "Hazards stall a pipeline: structural (resource conflict), data (an instruction needs a result not ready yet), and control (branches change the next instruction).",
      why: "Hazards are why real speedup falls short of $k$. Naming the three types and their fixes is the most-asked pipeline concept.",
      intuition: "The smooth one-per-cycle flow breaks whenever an instruction cannot proceed: the hardware is busy, the data is not computed, or the branch target is unknown.",
      analogy: {
        icon: "gear",
        label: "Line stoppages",
        text: "An assembly line halts if two stations need the same tool (structural), a part is not finished upstream (data), or the foreman has not decided which model to build next (control).",
        breaks: "Forwarding and prediction hide many hazards; they reduce stalls but cannot remove all of them.",
      },
      steps: [
        "Structural hazard: two stages need the same hardware resource at once.",
        "Data hazard: a later instruction reads a value a earlier one has not yet written.",
        "Types: RAW (true), WAR (anti), WAW (output) dependencies.",
        "Control hazard: a branch makes the next instruction uncertain (about 15-20% of instructions are branches, 60-70% taken).",
        "Fixes: forwarding/bypassing, stalls (bubbles, NOPs = number of stages flushed), and for branches: prediction (static or dynamic), delayed branch (fill the delay slot), prefetch the target, loop buffer.",
      ],
      example: "RAW: $I_2$ uses $R1$ that $I_1$ writes. Without forwarding $I_2$ must stall until $I_1$'s result is available; forwarding routes it early to avoid the bubble. (WAR holds when $D(I)\\cap R(J)\\ne\\varnothing$.)",
      misconception: "WAR and WAW are not true data dependencies; they come from reusing registers and can be removed by register renaming. Only RAW is a genuine value dependency. RAR (read after read) is NOT a hazard at all, a favourite MCQ.",
      rule: "Three hazards: structural (resource), data (RAW true / WAR / WAW; RAR is none), control (branch). Fix with forwarding, stalls, prediction.",
    },
  ],
  ch2: [
    {
      id: "memory-hierarchy",
      title: "The Memory Hierarchy",
      explainer: "memhierarchy",
      what: "Memory is layered from tiny-fast to huge-slow: registers, cache (L1/L2/L3), main memory (RAM), then disk, trading speed for capacity and cost.",
      why: "No single memory is both fast and large enough. The hierarchy gives the illusion of a big fast memory by keeping hot data near the CPU.",
      intuition: "Keep what you use most in the fastest, smallest level; push rarely-used data down to slower, larger levels.",
      analogy: {
        icon: "stack",
        label: "Desk, drawer, archive",
        text: "Registers are papers in your hand, cache is your desktop, RAM is the drawer, disk is the basement archive. Closer is faster but holds less.",
        breaks: "Movement between levels is automatic (hardware/OS), unlike a physical desk; the programmer mostly sees one address space.",
      },
      steps: [
        "Registers: fastest, a few hundred bytes.",
        "Cache L1/L2/L3: fast SRAM, kilobytes to megabytes.",
        "Main memory (RAM): larger, slower DRAM.",
        "Disk/SSD: huge, slowest.",
        "Each level caches the level below it.",
      ],
      example: "A value in L1 cache is read in a couple of cycles; the same value from disk is millions of cycles slower, hence the push to keep working data cached.",
      misconception: "The hierarchy does not make memory physically faster; it statistically keeps the data you need in a fast level, exploiting locality.",
      rule: "Registers $>$ cache $>$ RAM $>$ disk: faster and smaller as you go up, each caching the level below.",
    },
    {
      id: "locality-caching",
      title: "Locality and Cache Misses",
      explainer: "cache",
      what: "Caches work because programs show locality: temporal (reuse the same data soon) and spatial (use nearby data soon). Misses are compulsory, capacity or conflict.",
      why: "Cache behaviour dominates real performance. Understanding the kinds of misses tells you how to reduce them (bigger blocks, more associativity, larger cache).",
      intuition: "Fetch a block, not a byte, because you will likely use neighbours soon (spatial) and the same item again (temporal). A miss is any access not already cached.",
      analogy: {
        icon: "clock",
        label: "Keep what you just used",
        text: "Like leaving the books you are reading open on your desk (temporal) and grabbing the whole shelf section you are working through (spatial).",
        breaks: "Random access patterns have little locality, so caches help them far less.",
      },
      steps: [
        "Temporal locality: recently used data is used again soon.",
        "Spatial locality: data near recently used data is used soon.",
        "Cache stores blocks (lines), not single bytes.",
        "Hit = found in cache; miss = must fetch from below. Average access time $T_{avg}=h\\,t_c+(1-h)\\,t_m$.",
        "Misses (3 Cs): compulsory (first ref), capacity (cache too small), conflict (mapping clash, only in direct/set-associative).",
        "Block placement: direct-mapped (block to ONE line, $j=i\\bmod N$; fields Tag|Line|Word; conflict misses), fully associative (any line, content-addressable, Tag|Word, no conflict miss), set-associative (a set like direct, any line in the set like associative; Tag|Set|Word; resolves the direct-mapping drawback).",
      ],
      example: "The standard PYQ: cache access $100$ ns, main memory $1000$ ns, hit ratio $h=0.9$. $T_{avg}=0.9(100)+0.1(1000)=\\textbf{190 ns}$. (Another: $40,100,h=0.8 \\Rightarrow 52$ ns.)",
      misconception: "The three Cs are distinct, and reducing miss RATE differs from reducing miss PENALTY. Rate: larger block/cache, higher associativity, compiler optimisation. Penalty: an L2 cache, critical-word-first, a victim cache. Compulsory misses (first access) stay either way.",
      rule: "Locality (temporal + spatial) makes caches work; $T_{avg}=h\\,t_c+(1-h)t_m$; misses are compulsory/capacity/conflict; mapping is direct / associative / set-associative.",
    },
    {
      id: "virtual-memory",
      title: "Virtual Memory",
      explainer: "memhierarchy",
      what: "Virtual memory gives each process its own large address space, mapped to physical RAM (and disk) in fixed-size pages via a page table.",
      why: "It lets programs exceed physical RAM, isolates processes from each other, and simplifies memory allocation, all transparently to the programmer.",
      intuition: "The CPU issues virtual addresses; the page table translates them to physical frames, paging data in from disk on demand when it is not resident.",
      analogy: {
        icon: "map",
        label: "Address translation",
        text: "Virtual addresses are like apartment numbers a tenant uses; the building manager (page table) maps each to a real physical room, moving boxes from storage (disk) when needed.",
        breaks: "Translation costs time, so a TLB caches recent mappings; a page not in RAM triggers a (slow) page fault.",
      },
      steps: [
        "Each process sees a private virtual address space.",
        "Memory is divided into fixed-size pages.",
        "A page table maps virtual pages to physical frames.",
        "The TLB caches recent translations: a TLB hit gives the frame at once; a miss must walk the page table (an extra access), so with NO TLB every reference costs 2 accesses (page table + data).",
        "A reference to a non-resident page causes a page fault, fetched from disk. Paging uses fixed-size pages (internal fragmentation); segmentation uses variable segments (external fragmentation).",
      ],
      example: "TLB effective access time (a marquee PYQ): MM $100$ ns, TLB hit ratio $90\\%$; a found access takes $400$ ns and a not-found $500$ ns. $EAT = 0.9(400)+0.1(500)=\\textbf{410 ns}$. Pages/frames: a 48-bit virtual, 36-bit physical address with a 4096-byte ($2^{12}$) page gives $2^{36}$ virtual pages and $2^{24}$ physical frames.",
      misconception: "Virtual memory is about address mapping and isolation, not just 'using disk as RAM'. Excessive paging (thrashing) is what makes it slow, not the concept itself. Page size is a power of 2 so the address splits cleanly into (page number | offset) with no division.",
      rule: "Virtual memory = per-process paged address space mapped to physical frames by a page table (TLB-cached, disk-backed); no TLB = 2 accesses; $EAT=H(T_{TLB}+M)+(1-H)(T_{TLB}+\\text{walk}+M)$.",
    },
  ],
  ch3: [
    {
      id: "vector-processing",
      title: "Vector Processors",
      explainer: "pipeline",
      what: "A vector processor applies one instruction to a whole array of data at once, streaming the elements through a deeply pipelined functional unit.",
      why: "Scientific and graphics workloads do the same operation over huge arrays; one vector instruction replaces a loop and keeps the pipeline full, amortising overhead.",
      intuition: "Instead of fetch-decode-execute per element, issue one vector instruction and feed elements into the pipe back to back.",
      analogy: {
        icon: "gear",
        label: "One instruction, many data",
        text: "Like an assembly line that paints a whole row of cars with one command, rather than re-issuing the order for each car.",
        breaks: "Vector speedup needs long, regular arrays; short or irregular data leaves the pipeline underused.",
      },
      steps: [
        "Data is organised into vectors (arrays of elements).",
        "One vector instruction operates on the whole vector.",
        "Elements stream through a pipelined unit, one per cycle.",
        "Vector registers hold the operands; the vector length sets the count.",
        "This is SIMD: single instruction, multiple data.",
      ],
      example: "A vector add $C = A + B$ issues once and yields one sum per cycle after the pipeline fills, versus a scalar loop with per-iteration overhead.",
      misconception: "Vector processing is not multiple cores; it is one instruction over many data elements (SIMD), not many independent instructions.",
      rule: "Vector processor: one instruction over a whole array, streamed through a pipeline (SIMD).",
    },
    {
      id: "chaining",
      title: "Vector Pipelining and Chaining",
      explainer: "pipeline",
      what: "Chaining feeds the result of one vector operation directly into another as each element is produced, instead of waiting for the whole first vector to finish.",
      why: "It overlaps dependent vector operations, removing the stall where the second would otherwise wait for the first to complete entirely.",
      intuition: "As soon as element $i$ of the first result is ready, it flows into the second pipeline, so the two operations run almost concurrently.",
      analogy: {
        icon: "gear",
        label: "Forward each result",
        text: "Like a relay where each finished piece is handed to the next worker immediately, not after the whole batch is done.",
        breaks: "Chaining needs hardware paths to forward results; without them dependent vector operations serialise.",
      },
      steps: [
        "A vector operation produces results element by element.",
        "A dependent operation would normally wait for the full vector.",
        "Chaining forwards each element as it is produced.",
        "The two pipelines overlap, nearly halving the time.",
        "Several chained operations form one longer effective pipeline.",
      ],
      example: "For $D = (A + B) * C$, the multiply starts consuming sums as the adder produces them, not after the entire $A+B$ vector is finished.",
      misconception: "Chaining overlaps DEPENDENT operations by forwarding; it is not the same as simply adding more functional units.",
      rule: "Chaining: forward each vector element into the next dependent operation, overlapping the two.",
    },
    {
      id: "parallel-arch",
      title: "Superscalar, Superpipeline and VLIW",
      explainer: "pipeline",
      what: "Superscalar issues several instructions per cycle to multiple units; superpipelining uses more, finer stages clocked faster; VLIW packs several operations into one wide instruction chosen by the compiler.",
      why: "These are the main ways to exploit instruction-level parallelism beyond a single pipeline, each shifting the work of finding parallelism between hardware and compiler.",
      intuition: "Do more per cycle: issue multiple instructions (superscalar), clock a deeper pipeline faster (superpipeline), or let the compiler bundle independent operations (VLIW).",
      analogy: {
        icon: "gear",
        label: "More lanes vs finer steps",
        text: "Superscalar adds parallel lanes; superpipelining slices each lane into more, quicker steps; VLIW has the planner pre-bundle the work.",
        breaks: "Superscalar needs hardware to find parallelism at runtime; VLIW pushes that onto the compiler, which stalls if it guesses wrong.",
      },
      steps: [
        "Superscalar: issue several instructions per cycle to parallel units (hardware finds parallelism).",
        "Superpipeline: split stages into more, shorter stages clocked faster.",
        "VLIW: the compiler packs independent operations into one wide instruction.",
        "All exploit instruction-level parallelism.",
        "Hazards and dependencies still cap the achievable speedup.",
      ],
      example: "A 4-issue superscalar can retire up to 4 instructions per cycle; a VLIW bundles, say, 3 independent operations the compiler scheduled together.",
      misconception: "More parallelism is not free: dependencies, hazards and branch mispredictions hold the real instructions-per-cycle well below the issue width.",
      rule: "Superscalar (multi-issue), superpipeline (finer stages), VLIW (compiler-bundled) all exploit instruction-level parallelism.",
    },
  ],
  ch4: [
    {
      id: "flynn-taxonomy",
      title: "Flynn's Taxonomy",
      explainer: "flynn",
      what: "Flynn's taxonomy classifies a computer by how many instruction streams and data streams it processes at once: SISD, SIMD, MISD, MIMD.",
      why: "It is the standard vocabulary for parallel architectures, separating 'one operation on many data' (SIMD) from 'many independent programs' (MIMD).",
      intuition: "Ask two yes/no questions: one instruction stream or many? one data stream or many? The four answers are the four classes.",
      analogy: {
        icon: "grid",
        label: "Two yes/no axes",
        text: "A 2x2 grid: instructions (one or many) against data (one or many). Each cell is a kind of machine.",
        breaks: "Real systems mix categories (a multicore CPU with SIMD units is both MIMD and SIMD); the taxonomy is a coarse classification.",
      },
      steps: [
        "SISD: one instruction, one data, a sequential CPU.",
        "SIMD: one instruction, many data, vectors and GPUs.",
        "MISD: many instructions, one data, rare.",
        "MIMD: many instructions, many data, multicore and clusters.",
        "Classify by the number of concurrent instruction and data streams.",
      ],
      example: "A GPU running the same shader over thousands of pixels is SIMD; a 16-core server running different threads is MIMD. Key MCQ facts: SISD $=$ the Von Neumann machine; an array processor and Intel MMX are SIMD; MISD has essentially no practical use (sometimes fault-tolerant systems).",
      misconception: "SIMD is not the same as multicore. SIMD is one instruction over many data lanes; MIMD is many independent instruction streams.",
      rule: "Flynn (1966): classify by instruction streams x data streams, SISD / SIMD / MISD / MIMD. Array processor = SIMD, SISD = Von Neumann, MISD = no real use.",
    },
    {
      id: "parallel-models",
      title: "SIMD and MIMD in Practice",
      explainer: "flynn",
      what: "SIMD exploits data parallelism (the same operation over an array); MIMD exploits task parallelism (independent threads or processes), communicating via shared memory or message passing.",
      why: "Choosing SIMD vs MIMD shapes how you parallelise: lockstep over data, or independent cooperating tasks.",
      intuition: "If your work is 'do X to every element', SIMD fits. If it is 'run these different jobs', MIMD fits.",
      analogy: {
        icon: "crowd",
        label: "Marching vs teamwork",
        text: "SIMD is a marching band doing the same step in lockstep; MIMD is a team where each member does a different job and they coordinate.",
        breaks: "SIMD needs uniform, regular work; branches and irregular data waste its lanes.",
      },
      steps: [
        "SIMD: one instruction drives many data lanes in lockstep (vector/GPU).",
        "MIMD: each processor runs its own instruction stream.",
        "MIMD coordinates via shared memory or message passing.",
        "Data parallelism suits SIMD; task parallelism suits MIMD.",
        "Performance numericals: $CPU\\ time = IC\\times CPI\\times CCT$; $CPI_{avg}=\\dfrac{\\sum CPI_i\\,I_i}{IC}$; $MIPS=\\dfrac{f}{CPI_{avg}\\times 10^6}$. Amdahl: $Speedup=\\dfrac{1}{(1-f)+f/S}\\to \\dfrac{1}{1-f}$.",
      ],
      example: "Summing a huge array is SIMD; a web server across many cores is MIMD. CPI/MIPS PYQ (50 MHz; mix giving CPI $=1.6$): $MIPS=50/1.6=\\textbf{31.25}$. Amdahl PYQ: 10% of work is sequential and you want a $5\\times$ speedup: $5=\\dfrac{1}{0.1+0.9/n}\\Rightarrow n=\\textbf{9 processors}$.",
      misconception: "More cores (MIMD) is not always better than wide SIMD; for regular array math, SIMD lanes are far cheaper than full cores. And Amdahl caps speedup at $1/(1-f)$ no matter how many processors you add.",
      rule: "SIMD = data parallelism (lockstep over arrays); MIMD = task parallelism (independent streams). $MIPS=f/(CPI\\cdot10^6)$; Amdahl $\\to 1/(1-f)$.",
    },
  ],
  ch5: [
    {
      id: "risc-cisc",
      title: "RISC vs CISC",
      explainer: "risccisc",
      what: "RISC uses a small set of simple, fixed-length instructions that pipeline cleanly; CISC uses many complex, variable-length instructions that do more per instruction.",
      why: "It is the central architecture trade-off: where to put complexity, in simple hardware plus a clever compiler (RISC), or in rich hardware instructions (CISC).",
      intuition: "RISC does little per instruction but runs them fast and uniformly; CISC does a lot per instruction but each is slower and irregular.",
      analogy: {
        icon: "balance",
        label: "Where complexity lives",
        text: "RISC hands the compiler simple Lego bricks; CISC gives pre-built complex parts. Same buildings, different division of labour.",
        breaks: "The line blurs: modern x86 (CISC) decodes instructions into RISC-like micro-ops internally.",
      },
      steps: [
        "RISC: few simple, fixed-length instructions; load/store architecture; many registers.",
        "CISC: many complex, variable-length instructions; can operate on memory directly.",
        "RISC keeps cycles-per-instruction near 1 and pipelines well.",
        "CISC packs more work per instruction, fewer instructions per program.",
        "RISC pushes complexity to the compiler; CISC keeps it in hardware.",
      ],
      example: "ARM, MIPS, SPARC are RISC; x86 is CISC (internally x86 decodes into RISC-like micro-ops). Specifics for the comparison table: RISC has CPI $\\approx 1$, a HARDWIRED control unit, split I/D cache, many registers (32-192), and few addressing modes (3-5); CISC has CPI $2$ to $15$, a MICROPROGRAMMED (ROM) control unit, a unified cache, few registers, and many addressing modes (12-24).",
      misconception: "Neither is simply 'better'. RISC trades more instructions for faster, uniform ones; the right choice depends on workload, compilers and power goals. Note: a small register file is NOT a RISC trait, RISC has MANY registers.",
      rule: "RISC = simple fixed instructions, CPI~1, hardwired, many registers + compiler complexity; CISC = rich variable instructions, CPI 2-15, microprogrammed ROM + hardware complexity.",
    },
    {
      id: "instruction-design",
      title: "Instruction-Set Trade-offs",
      explainer: "risccisc",
      what: "Instruction-set design balances how much each instruction does against how fast and uniformly it executes, affecting pipelining, code size and compiler effort.",
      why: "The RISC/CISC trade-off recurs everywhere: simpler, regular instructions pipeline better but need more of them; richer instructions shrink code but complicate the hardware.",
      intuition: "Every feature added to an instruction set helps some programs but costs decode and pipeline complexity. Designers pick the sweet spot for their goals.",
      analogy: {
        icon: "balance",
        label: "Simple-and-many vs rich-and-few",
        text: "Like choosing between many small tools or a few multitools: small tools are predictable and fast; multitools do more but are bulkier.",
        breaks: "Fixed-length simple encodings pipeline best but waste space; dense variable encodings save memory but slow decoding.",
      },
      steps: [
        "Fewer, simpler instructions: easy decode, clean pipeline, larger code.",
        "Richer instructions: smaller code, harder decode, variable timing.",
        "Fixed-length encoding helps pipelining; variable-length saves space.",
        "More addressing modes add power but complicate the hardware.",
        "Compilers must exploit whatever the instruction set offers.",
      ],
      example: "A load/store RISC needs explicit load, add, store; a CISC add-to-memory does it in one instruction but takes several cycles.",
      misconception: "A 'powerful' instruction is not automatically faster; if it stalls the pipeline or is rarely used, simpler instructions can win overall.",
      rule: "Instruction-set design trades work-per-instruction against fast, uniform execution (pipelining, code size, compiler load).",
    },
  ],
  ch6: [
    {
      id: "ipc-models",
      title: "Interprocess Communication",
      explainer: "ipc",
      what: "Two cooperating processes exchange data either through a shared memory region they both access, or by passing messages through a channel managed by the system.",
      why: "Processes have separate address spaces by default, so to cooperate (a producer feeding a consumer, parallel tasks sharing results) they need a deliberate communication mechanism.",
      intuition: "Either both processes point at one common region (fast, but they can clash), or they hand copies back and forth through a channel (safe, but slower).",
      analogy: {
        icon: "balance",
        label: "Shared whiteboard vs passing notes",
        text: "Shared memory is one whiteboard both people write on; message passing is folding a note and handing it over. The whiteboard is quick but two hands can collide.",
        breaks: "A real shared-memory region needs hardware cache coherence and locks; the whiteboard picture hides that cost.",
      },
      steps: [
        "Shared memory: the system maps one region into both processes; reads and writes are ordinary memory accesses, very fast.",
        "Because both can write at once, access must be synchronised (locks, semaphores) to avoid races.",
        "Message passing: a process sends data through a channel (pipe, queue, socket); the system copies it to the receiver.",
        "No shared state to corrupt, but copying and kernel involvement add overhead.",
        "Choose shared memory for speed within one machine, messages for isolation or across machines.",
        "At the system level this is multiprocessor vs multicomputer: a MULTIPROCESSOR has one shared memory + LOAD/STORE communication and is tightly coupled (e.g. SGI Origin); a MULTICOMPUTER has private memory per CPU + message passing and is loosely coupled (e.g. a Beowulf cluster). Shared-memory machines are UMA (same access time for all, an SMP) or NUMA (local memory faster).",
      ],
      example: "A producer-consumer buffer in shared memory is fast but needs a lock; the same pair using a pipe is simpler and safer but copies every item. Multiprocessors connect through interconnection networks: static (ring, mesh, hypercube with $2^n$ nodes) or dynamic (bus, crossbar with $N^2$ crosspoints, omega). An $N\\times N$ omega network has $\\log_2 N$ stages of $2\\times2$ switches (so $N=64$ gives 6 stages).",
      misconception: "Shared memory is not automatically 'better' for being faster; without synchronisation it produces corrupted data, which is worse than slower-but-correct messages. And tightly coupled (shared memory) is not always superior to loosely coupled, clusters are cheaper, fault-tolerant and scale further.",
      rule: "Multiprocessor (shared memory, LOAD/STORE, tightly coupled, UMA/NUMA) vs multicomputer (private memory, message passing, loosely coupled); networks: static (mesh/hypercube) or dynamic (crossbar/omega, $\\log_2 N$ stages).",
    },
    {
      id: "synchronization",
      title: "Synchronising Shared Access",
      explainer: "ipc",
      what: "When processes share memory, a synchronisation mechanism (mutex, semaphore) ensures only one updates a critical section at a time, so concurrent writes do not interleave into garbage.",
      why: "Unsynchronised access to shared data is the classic source of race conditions, where the final result depends on unpredictable timing between processes.",
      intuition: "Put a lock on the shared region: a process must acquire it before touching the data and release it after, so updates happen one clean turn at a time.",
      analogy: {
        icon: "balance",
        label: "A single talking stick",
        text: "Only the person holding the talking stick may write; everyone else waits their turn. The stick guarantees no two write at once.",
        breaks: "Locks can deadlock if processes wait on each other in a cycle; the talking stick picture does not show that hazard.",
      },
      steps: [
        "Identify the critical section: code that touches shared data.",
        "Acquire a lock (or decrement a semaphore) before entering it.",
        "Do the update while holding the lock; no other process can enter.",
        "Release the lock (increment the semaphore) on exit so a waiter proceeds.",
        "Keep critical sections short to limit how long others wait.",
        "Hardware version, cache coherence: in a multiprocessor each CPU caches shared data, so one CPU's write leaves the others STALE. Solved by snooping (caches watch the bus and invalidate/update) or a central directory; MESI (Modified/Exclusive/Shared/Invalid) is the standard write-invalidate, write-back protocol.",
      ],
      example: "Two processes incrementing a shared counter without a lock can both read 5, both write 6, losing an update; a lock around the increment fixes it. The same staleness at the cache level is the coherence problem, worst in asynchronous parallel execution across processors.",
      misconception: "Locks do not make code faster; they make it correct. Over-locking serialises everything and kills the parallelism shared memory was meant to give.",
      rule: "Guard every shared critical section with mutual exclusion (locks/semaphores); at the hardware level, keep caches consistent with a coherence protocol (snooping/directory, MESI).",
    },
  ],
};
