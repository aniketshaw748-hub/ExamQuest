// Authored Explanation-Ladder lessons for Formal Language & Automata Theory.
// Standard theory (the verified PYQ answers still come from automata.json untouched).

export const TEACH_AUTOMATA = {
  ch1: [
    {
      id: "alphabets-strings-languages",
      title: "Alphabets, Strings and Languages",
      explainer: "langset",
      what: "An alphabet $\\Sigma$ is a finite set of symbols; a string is a finite sequence over $\\Sigma$; a language is any set of strings, a subset of $\\Sigma^*$.",
      why: "Every machine in this course accepts or rejects strings, so you first need precise words for symbols, strings and the collections of strings they form.",
      intuition: "Symbols are letters, strings are words, a language is a (possibly infinite) dictionary of allowed words. $\\Sigma^*$ is every word you can spell, including the empty one.",
      analogy: {
        icon: "stack",
        label: "Letters, words, dictionary",
        text: "$\\Sigma$ is the alphabet, a string is a word, a language is a dictionary listing which words count as valid.",
        breaks: "Natural dictionaries are finite; formal languages are often infinite (e.g. all strings of even length).",
      },
      steps: [
        "Alphabet $\\Sigma$: a finite set of symbols, e.g. $\\{0,1\\}$.",
        "String: a finite sequence of symbols; $\\varepsilon$ is the empty string.",
        "$\\Sigma^*$: all strings over $\\Sigma$; $\\Sigma^+$ excludes $\\varepsilon$.",
        "Language $L$: any subset of $\\Sigma^*$.",
        "Operations on languages: union, concatenation, Kleene star.",
      ],
      example: "Over $\\Sigma=\\{0,1\\}$, the language of strings ending in 0 is $\\{0,00,10,000,\\ldots\\}$, an infinite subset of $\\Sigma^*$.",
      misconception: "A language is a set of strings, not a single string. And $\\varepsilon$ (the empty string) is different from the empty language $\\varnothing$.",
      rule: "Alphabet $\\Sigma$ (finite) $\\to$ strings (finite sequences) $\\to$ language (any subset of $\\Sigma^*$).",
    },
    {
      id: "grammars",
      title: "Grammars and Derivations",
      explainer: "derivation",
      what: "A grammar $(V,\\Sigma,P,S)$ generates a language: starting from $S$, apply production rules $P$ until only terminals remain.",
      why: "Grammars are the generative side of languages (machines recognise, grammars produce). The type of rules allowed fixes how powerful the language is.",
      intuition: "Begin with the start symbol and keep rewriting non-terminals using the rules until the string is all terminals. The set of all such strings is the generated language.",
      analogy: {
        icon: "ladder",
        label: "Rewrite rules",
        text: "Like sentence templates: start with 'Sentence', expand to 'Subject Verb Object', expand each part, until every slot is a concrete word.",
        breaks: "Natural grammar is informal; formal grammars are exact rewrite systems with no ambiguity about what a rule does.",
      },
      steps: [
        "$V$ = non-terminals (variables), $\\Sigma$ = terminals, $S$ = start symbol.",
        "Productions $P$ rewrite the left side into the right side.",
        "A derivation applies productions until only terminals remain.",
        "The language is every terminal string derivable from $S$.",
        "Restricting the form of $P$ gives the Chomsky hierarchy.",
      ],
      example: "$S\\to 0S1 \\mid \\varepsilon$ generates $\\{0^n1^n : n\\ge 0\\}$: each step adds a matching 0 and 1.",
      misconception: "A grammar generates strings; an automaton recognises them. They describe the same language from two directions, but are not the same object.",
      rule: "Grammar = start symbol + rewrite rules; the language is all terminal strings it can derive.",
    },
    {
      id: "chomsky-hierarchy",
      title: "The Chomsky Hierarchy",
      explainer: "chomsky",
      what: "Four nested language classes by grammar restriction: regular $\\subset$ context-free $\\subset$ context-sensitive $\\subset$ recursively enumerable.",
      why: "The hierarchy tells you which machine you need: a finite automaton for regular, a pushdown automaton for context-free, a Turing machine for recursively enumerable.",
      intuition: "Loosen the grammar rules and you climb to more powerful languages, but you also need a more powerful machine and lose decidability guarantees.",
      analogy: {
        icon: "stack",
        label: "Nested power levels",
        text: "Each level sits inside the next: every regular language is context-free, every context-free language is context-sensitive, and so on outward.",
        breaks: "The containments are strict: some context-free languages (like $0^n1^n$) are not regular, so the levels are genuinely different.",
      },
      steps: [
        "Type 3 (regular): rules $A\\to aB$ or $A\\to a$; machine = finite automaton.",
        "Type 2 (context-free): rules $A\\to \\gamma$; machine = pushdown automaton.",
        "Type 1 (context-sensitive): non-contracting rules; machine = linear-bounded automaton.",
        "Type 0 (recursively enumerable): unrestricted; machine = Turing machine.",
        "Each class strictly contains the one below.",
      ],
      example: "$0^n1^n$ is context-free but not regular; $0^n1^n2^n$ is context-sensitive but not context-free. Regular languages are the weakest, recognised by finite automata.",
      misconception: "More powerful is not always better: regular languages are decidable and efficient, while recursively enumerable ones can be undecidable.",
      rule: "Regular $\\subset$ context-free $\\subset$ context-sensitive $\\subset$ recursively enumerable, matched by FA, PDA, LBA, TM.",
    },
  ],
  ch2: [
    {
      id: "dfa",
      title: "Deterministic Finite Automata",
      explainer: "automaton",
      what: "A DFA is $(Q,\\Sigma,\\delta,q_0,F)$: states, alphabet, a total transition function, a start state and accepting states. It accepts a string if reading it ends in $F$.",
      why: "The DFA is the simplest machine and the exact recogniser for regular languages. Everything else in this chapter reduces to or builds on it.",
      intuition: "A DFA is a controlled walk: you sit in a state, read one symbol, and the transition tells you exactly which state to move to. No choices, no memory beyond the current state.",
      analogy: {
        icon: "map",
        label: "One-way turnstiles",
        text: "Each state is a room; each symbol opens exactly one door to the next room. Reading the whole string lands you in some room, accept if it is a goal room.",
        breaks: "A DFA has no stack or counter, so it cannot remember how many symbols it has seen, only a finite amount of state.",
      },
      steps: [
        "States $Q$, alphabet $\\Sigma$, start $q_0$, accepting set $F$.",
        "Transition $\\delta: Q\\times\\Sigma \\to Q$ is total and single-valued.",
        "Process the string symbol by symbol from $q_0$.",
        "Accept iff the final state is in $F$.",
        "The language recognised is exactly a regular language.",
      ],
      example: "A 2-state DFA over $\\{0,1\\}$ that toggles state on each 1 accepts strings with an even number of 1s.",
      misconception: "Deterministic means exactly one transition per (state, symbol), and the function is total: every case must be defined, often via a dead (trap) state.",
      rule: "DFA = one transition per symbol, accept if you end in $F$. DFAs recognise exactly the regular languages.",
    },
    {
      id: "nfa",
      title: "Nondeterministic Finite Automata",
      explainer: "automaton",
      what: "An NFA may have several transitions (or none) per symbol and may take $\\varepsilon$-moves. It accepts if some path leads to an accepting state.",
      why: "NFAs are far easier to design (and to build from regular expressions), yet recognise exactly the same languages as DFAs.",
      intuition: "An NFA explores all choices in parallel. The string is accepted if at least one of those parallel paths ends in an accepting state.",
      analogy: {
        icon: "map",
        label: "Try every path",
        text: "Like a maze where you clone yourself at every fork and take all paths at once; you escape if any clone reaches the exit.",
        breaks: "Nondeterminism is a design convenience, not extra power: an NFA can always be converted to an equivalent DFA.",
      },
      steps: [
        "Transition $\\delta: Q\\times(\\Sigma\\cup\\{\\varepsilon\\}) \\to 2^Q$ (a set of states).",
        "Several or zero next states are allowed.",
        "$\\varepsilon$-moves change state without reading a symbol.",
        "Accept if any computation path ends in $F$.",
        "NFAs and DFAs recognise the same class (regular languages).",
      ],
      example: "An NFA for 'strings ending in 01' can stay in the start state on any symbol and branch out to match the final 0 then 1, no dead-state bookkeeping needed.",
      misconception: "Nondeterminism does not add power for finite automata; it only makes machines smaller and easier to design. The recognised class is unchanged.",
      rule: "NFA: many/zero/$\\varepsilon$ moves, accept if some path does. Same power as a DFA.",
    },
    {
      id: "nfa-to-dfa",
      title: "Subset Construction (NFA to DFA)",
      explainer: "subset",
      what: "Every NFA converts to an equivalent DFA whose states are sets of NFA states (the subset construction).",
      why: "It proves NFAs and DFAs are equally powerful, and it is the standard way to turn an easy-to-design NFA into a deterministic machine for implementation.",
      intuition: "Track the set of all NFA states you could currently be in. That set is one DFA state; reading a symbol moves the whole set at once.",
      analogy: {
        icon: "grid",
        label: "Track the possibilities",
        text: "Instead of following one maze clone, keep a checklist of every room any clone occupies. Update the whole checklist per symbol.",
        breaks: "The DFA can have up to $2^n$ states for an $n$-state NFA, an exponential blow-up in the worst case.",
      },
      steps: [
        "DFA start state = $\\varepsilon$-closure of the NFA start.",
        "Each DFA state is a set of NFA states.",
        "On symbol $a$, move to the $\\varepsilon$-closure of all $a$-successors.",
        "A DFA state is accepting if it contains any NFA accepting state.",
        "Stop when no new subsets appear (at most $2^n$).",
      ],
      example: "A 3-state NFA can expand to a DFA with up to $2^3=8$ states, each labelled by which NFA states it represents.",
      misconception: "The subset construction can blow up exponentially, but it always terminates: there are only finitely many subsets of the NFA states.",
      rule: "DFA states = sets of NFA states; move sets via $\\varepsilon$-closure. Worst case $2^n$ states.",
    },
    {
      id: "regular-expressions",
      title: "Regular Expressions and Kleene's Theorem",
      explainer: "regex",
      what: "Regular expressions build languages from $\\varnothing$, $\\varepsilon$ and symbols using union ($+$), concatenation and Kleene star. Kleene's theorem: regex and finite automata describe exactly the same languages.",
      why: "Regular expressions are the algebraic, human-friendly way to specify regular languages; the equivalence with automata is the backbone of the whole regular theory.",
      intuition: "A regular expression is a recipe; a finite automaton is a machine. Kleene's theorem says every recipe has a machine and every machine has a recipe.",
      analogy: {
        icon: "gear",
        label: "Recipe equals machine",
        text: "$a^*b$ is a pattern 'any number of a's then a b'. You can build a small automaton that accepts exactly those strings, and vice versa.",
        breaks: "Only regular languages have regular expressions; $0^n1^n$ has no regex because it is not regular.",
      },
      steps: [
        "Base: $\\varnothing$, $\\varepsilon$, and each symbol $a$ are regular expressions.",
        "Combine with union $r+s$, concatenation $rs$, and star $r^*$.",
        "Star means zero or more repetitions.",
        "Kleene: regex $\\Leftrightarrow$ NFA $\\Leftrightarrow$ DFA (all describe regular languages).",
        "Convert regex $\\to$ NFA (Thompson) or DFA $\\to$ regex (state elimination).",
      ],
      example: "$(0+1)^*01$ is all strings ending in 01. $a^*$ is zero or more a's, including $\\varepsilon$.",
      misconception: "$r^*$ includes the empty string (zero copies). And $+$ here means 'or' (union), not arithmetic addition.",
      rule: "Regex = $\\varnothing,\\varepsilon,a$ closed under $+$, concatenation, $*$. Same expressive power as finite automata.",
    },
    {
      id: "pumping-lemma",
      title: "The Pumping Lemma for Regular Languages",
      explainer: "pumping",
      what: "Every regular language has a pumping length $p$: any string of length $\\ge p$ splits as $xyz$ with $|xy|\\le p$, $|y|\\ge 1$, and $xy^iz$ in the language for all $i\\ge 0$.",
      why: "It is the standard tool to prove a language is NOT regular: if pumping fails for every split, no finite automaton can recognise it.",
      intuition: "A DFA with $p$ states must repeat a state on a long string, creating a loop $y$ you can repeat any number of times and still be accepted.",
      analogy: {
        icon: "ladder",
        label: "A loop you can repeat",
        text: "On a long enough walk through finitely many rooms you must revisit a room, so the loop between visits can be taken 0, 1, 2, ... times and you still finish where you did.",
        breaks: "The lemma is one-directional: it can disprove regularity, but passing it does not prove a language is regular.",
      },
      steps: [
        "Assume the language is regular with pumping length $p$.",
        "Pick a string $s$ in the language with $|s|\\ge p$.",
        "For every split $s=xyz$ with $|xy|\\le p$, $|y|\\ge 1$,",
        "find some $i$ with $xy^iz$ NOT in the language.",
        "That contradiction shows the language is not regular.",
      ],
      example: "$\\{0^n1^n\\}$ is not regular: pump $y$ (all 0s) and the counts of 0s and 1s no longer match, so $xy^2z$ leaves the language.",
      misconception: "The pumping lemma proves non-regularity, never regularity. You must show pumping fails for the cleverly chosen string, for every valid split.",
      rule: "Long strings in a regular language have a repeatable middle $y$; if no split pumps, the language is not regular.",
    },
    {
      id: "closure-minimization",
      title: "Closure Properties and DFA Minimization",
      explainer: "minimize",
      what: "Regular languages are closed under union, intersection, complement, concatenation and star. Every DFA has a unique minimal equivalent DFA.",
      why: "Closure lets you build complex regular languages from simple ones; minimization gives the smallest machine and a canonical form for testing equivalence.",
      intuition: "Closure: combine automata (product machine for intersection, swap accepting states for complement). Minimization: merge states that behave identically on all future input.",
      analogy: {
        icon: "sort",
        label: "Merge identical states",
        text: "Two states are the same if no string ever distinguishes them (one accepts, the other rejects). Fuse all such indistinguishable states into one.",
        breaks: "Minimization is unique for DFAs; NFAs have no such canonical minimal form.",
      },
      steps: [
        "Closed under union/intersection (product automaton).",
        "Closed under complement (swap accepting and non-accepting in a DFA).",
        "Closed under concatenation and Kleene star.",
        "Minimize: remove unreachable states, then merge indistinguishable states (Myhill-Nerode).",
        "The result is the unique minimal DFA.",
      ],
      example: "To intersect two regular languages, run both DFAs in parallel (the product DFA) and accept when both accept. Minimizing it merges states no string can tell apart.",
      misconception: "Complement is easy only for a DFA (swap the accepting set); doing it on an NFA by swapping states is wrong. Convert to a DFA first.",
      rule: "Regular languages are closed under the boolean and regex operations; each DFA has one minimal form (merge indistinguishable states).",
    },
    {
      id: "mealy-moore",
      title: "Mealy and Moore Machines",
      explainer: "automaton",
      what: "A finite automaton with output. A Moore machine's output depends only on the current state; a Mealy machine's output depends on the current state AND the current input.",
      why: "Designing a sequence detector and converting Mealy to/from Moore are repeated exam questions.",
      intuition: "Moore attaches an output to each STATE; Mealy attaches it to each TRANSITION (arrow), so Mealy can react one input symbol sooner.",
      analogy: {
        icon: "gear",
        label: "Traffic light vs coin display",
        text: "A traffic light shows a colour because of which state it is in (Moore); a vending display that changes the instant you drop a coin reacts to the input event (Mealy).",
        breaks: "Both are still finite automata, equally powerful; only where the output sits differs.",
      },
      steps: [
        "Moore: output $\\lambda(\\text{state})$; for an input of length $n$ the output has length $n+1$.",
        "Mealy: output $\\lambda(\\text{state}, \\text{input})$; output length $=$ input length.",
        "Sequence detector: one state per matched-prefix length; on overlap, fall back to the longest still-valid prefix.",
        "Moore to Mealy: move each state's output onto its incoming transitions.",
        "Mealy to Moore: split each state by the distinct outputs on its incoming edges.",
      ],
      example: "A detector for the pattern 1101 (with overlap) outputs 1 only on the transition that completes 1101. The Mealy and Moore versions accept the same strings; they differ only in where the output is written and the off-by-one in output length.",
      misconception: "Mealy and Moore are equally powerful (both finite automata). The only differences are output-on-transition vs output-on-state and the length-$n$ vs length-$(n+1)$ output.",
      rule: "Moore output $=f(\\text{state})$ (length $n+1$); Mealy output $=f(\\text{state},\\text{input})$ (length $n$); convertible either way.",
    },
  ],
  ch3: [
    {
      id: "cfg",
      title: "Context-Free Grammars",
      explainer: "derivation",
      what: "A context-free grammar has productions $A \\to \\gamma$ where the left side is a single variable; it generates exactly the context-free languages.",
      why: "CFGs describe nested, recursive structure (balanced brackets, arithmetic, programming-language syntax) that regular expressions cannot.",
      intuition: "Start from $S$ and replace any variable by the right side of one of its rules, regardless of context, until only terminals remain.",
      analogy: {
        icon: "ladder",
        label: "Rewrite rules",
        text: "Like expanding a template: $E \\to E+E$ or $E \\to \\text{num}$, recursively, until every placeholder is a concrete token.",
        breaks: "Context-free means a variable expands the same way whatever surrounds it; context-sensitive grammars drop that freedom.",
      },
      steps: [
        "Variables, terminals, a start symbol $S$, productions $A \\to \\gamma$.",
        "The left side is always a single variable (that is what 'context-free' means).",
        "Derive: replace a variable using one of its rules.",
        "Repeat until only terminals remain.",
        "The language is every terminal string derivable from $S$.",
      ],
      example: "$S \\to 0S1 \\mid \\varepsilon$ generates $\\{0^n 1^n\\}$, which no regular expression can. Balanced parentheses: $S \\to (S)S \\mid \\varepsilon$.",
      misconception: "Context-free is about the rule shape (single variable on the left), not meaning. A CFG can still be ambiguous or generate a non-regular language.",
      rule: "CFG: productions $A \\to \\gamma$; generates the context-free languages, recognised by pushdown automata.",
    },
    {
      id: "parse-trees",
      title: "Parse Trees and Ambiguity",
      explainer: "parsetree",
      what: "A parse tree shows how a string is derived: the root is $S$, internal nodes are variables expanded by rules, and the leaves spell the string. Two different parse trees for one string means the grammar is ambiguous.",
      why: "Parse trees capture syntactic structure (precedence, nesting). Ambiguity breaks compilers, so grammars are designed to avoid it.",
      intuition: "Each rule application $A \\to \\gamma$ becomes a node $A$ with children $\\gamma$. The leaves read left to right give back the string (the yield).",
      analogy: {
        icon: "treeIcon",
        label: "Sentence diagram",
        text: "Like diagramming a sentence into subject and predicate: the tree shows which parts group together.",
        breaks: "One string can have several diagrams if the grammar is ambiguous, which is exactly what you try to prevent.",
      },
      steps: [
        "Root = start symbol $S$.",
        "Expanding $A \\to \\gamma$ adds children $\\gamma$ under $A$.",
        "Leaves are terminals; left to right they spell the string (the yield).",
        "Two different parse trees for one string means the grammar is ambiguous.",
        "Ambiguity is usually fixed by rewriting the grammar (e.g. encoding precedence).",
      ],
      example: "$S \\to 0S1 \\mid \\varepsilon$ gives a unique parse tree for each $0^n1^n$. But $E \\to E+E \\mid E*E \\mid \\text{num}$ is ambiguous: $1+2*3$ parses two ways.",
      misconception: "Ambiguity is a property of the grammar, not the language. The same language often has both ambiguous and unambiguous grammars.",
      rule: "Parse tree = derivation structure; two parse trees for one string = ambiguous grammar.",
    },
    {
      id: "cfl-pumping",
      title: "Pumping Lemma for Context-Free Languages",
      explainer: "cflpump",
      what: "Long strings split as $s = uvxyz$ with $|vxy| \\le p$, $|vy| \\ge 1$, and $uv^i x y^i z$ stays in the language for all $i$. If no split works, the language is not context-free.",
      why: "It is the tool to prove a language is NOT context-free (so no PDA recognises it), the CFL analogue of the regular pumping lemma.",
      intuition: "A long enough derivation repeats a variable, giving a sub-tree you can paste in extra times, pumping two coupled pieces $v$ and $y$. Because $|vxy| \\le p$, they touch at most two symbol blocks.",
      analogy: {
        icon: "ladder",
        label: "Two pumps at once",
        text: "Unlike the regular lemma's single loop, you repeat two coupled segments $v$ and $y$ together. For $0^n1^n2^n$ that lifts at most two of the three counts, never all three.",
        breaks: "It disproves context-freeness; passing it does not prove a language is context-free.",
      },
      steps: [
        "Assume $L$ is context-free with pumping length $p$.",
        "Pick $s \\in L$ with $|s| \\ge p$.",
        "For every split $s = uvxyz$ ($|vxy| \\le p$, $|vy| \\ge 1$),",
        "find an $i$ with $uv^i x y^i z \\notin L$.",
        "The contradiction shows $L$ is not context-free.",
      ],
      example: "$\\{0^n 1^n 2^n\\}$ is not context-free: $v$ and $y$ sit in at most two blocks, so pumping unbalances the third count. $\\{0^n 1^n\\}$ IS context-free (a PDA matches them).",
      misconception: "You pump $v$ and $y$ TOGETHER (same $i$), not independently. And $|vxy| \\le p$ forces them close, which is the heart of the argument.",
      rule: "CFL pumping: $s = uvxyz$, repeat $v$ and $y$ together; if no split survives, $L$ is not context-free.",
    },
    {
      id: "cnf-gnf",
      title: "Normal Forms: CNF and GNF",
      explainer: "derivation",
      what: "Chomsky Normal Form (CNF) restricts every production to $A\\to BC$ (two variables) or $A\\to a$ (one terminal). Greibach Normal Form (GNF) makes every production $A\\to a\\,\\alpha$ (a terminal then variables only). Any context-free grammar can be converted to either.",
      why: "'Convert this CFG to CNF / GNF' is a guaranteed 15-mark question; CNF underlies the CYK parsing algorithm and GNF underlies PDA construction.",
      intuition: "First clean the grammar (remove the empty, unit and useless productions), then reshape the right-hand sides into the required pattern.",
      analogy: {
        icon: "checklist",
        label: "Standardising a recipe",
        text: "Rewrite every step so it is either 'combine exactly two things' (CNF) or 'start with one fixed action, then continue' (GNF), without changing the dish.",
        breaks: "Conversion can blow up the number of productions; the language is preserved but the grammar gets larger.",
      },
      steps: [
        "Simplify first, in this order: remove $\\varepsilon$-productions, then unit productions, then useless symbols.",
        "CNF: replace each terminal inside a long right side by a new variable (TERM), then binarise right sides longer than 2 into a chain of $A\\to BC$ rules (BIN).",
        "GNF: order the variables; substitute so each $A_i\\to A_j\\ldots$ has $j>i$; remove immediate left recursion; back-substitute so every right side starts with a terminal.",
        "Left-recursion removal: $A\\to A\\alpha \\mid \\beta$ becomes $A\\to \\beta A'$, $A'\\to \\alpha A' \\mid \\varepsilon$.",
        "CNF target: $A\\to BC$ or $A\\to a$. GNF target: $A\\to a$ then variables only.",
      ],
      example: "$S\\to aSb \\mid ab$ in CNF (with $D_a\\to a$, $D_b\\to b$): $S\\to D_a X$, $X\\to S D_b$, $S\\to D_a D_b$. For GNF, the key tool is the left-recursion rule $A\\to A\\alpha\\mid\\beta \\Rightarrow A\\to\\beta A',\\ A'\\to\\alpha A'\\mid\\varepsilon$.",
      misconception: "You must simplify in the order $\\varepsilon$ then unit then useless. And the left-recursion removal for GNF deliberately introduces a new variable with an $\\varepsilon$-production, that is expected, not an error.",
      rule: "CNF: $A\\to BC$ or $A\\to a$; GNF: $A\\to a$ then variables (remove left recursion); always simplify ($\\varepsilon$, unit, useless) first.",
    },
  ],
  ch4: [
    {
      id: "pda",
      title: "Pushdown Automata",
      explainer: "pda",
      what: "A pushdown automaton is a finite automaton plus a stack. The stack gives it unbounded memory, so it recognises exactly the context-free languages.",
      why: "Finite automata cannot count (they fail $0^n1^n$). The stack lets a PDA remember an unbounded amount, matching the power of context-free grammars.",
      intuition: "On each step the PDA reads an input symbol (or $\\varepsilon$), looks at the top of the stack, and may push or pop. The stack is last-in-first-out memory.",
      analogy: {
        icon: "stack",
        label: "A stack of plates",
        text: "Push a plate for each 0, remove one for each 1; if the stack is empty exactly when the input ends, the counts matched.",
        breaks: "The stack only shows its top; a PDA cannot inspect the middle of its memory, which is why it cannot do $0^n1^n2^n$.",
      },
      steps: [
        "Components: states, input alphabet, stack alphabet $\\Gamma$, start stack symbol $Z$.",
        "A move reads (state, input symbol or $\\varepsilon$, stack top) and replaces the top.",
        "Push by replacing the top with several symbols; pop by replacing it with none.",
        "Accept by final state, or by empty stack.",
        "PDAs recognise exactly the context-free languages.",
      ],
      example: "For $\\{0^n 1^n\\}$: push $X$ on each 0, pop an $X$ on each 1, accept when the stack is empty. The stack counts the 0s.",
      misconception: "A PDA can only touch the top of its stack (LIFO). That single restriction is why PDAs are strictly weaker than Turing machines.",
      rule: "PDA = finite control + a stack; recognises exactly the context-free languages.",
    },
    {
      id: "pda-acceptance",
      title: "Acceptance by Final State or Empty Stack",
      explainer: "pda",
      what: "A PDA can accept two equivalent ways: by final state (end in an accepting state) or by empty stack (the stack empties as the input is consumed). Both define the same language class.",
      why: "Different constructions favour one mode; knowing they are interchangeable lets you pick whichever is convenient.",
      intuition: "Empty-stack acceptance says 'I cancelled all my memory exactly as the input ended'; final-state says 'I finished in a good state'. Either can simulate the other.",
      analogy: {
        icon: "stack",
        label: "Two finish lines",
        text: "Win by crossing the line (final state) or by clearing your tray (empty stack). A small gadget converts one rule into the other.",
        breaks: "For one fixed PDA the two modes can accept different strings; the equivalence is between the language classes, via a construction.",
      },
      steps: [
        "By final state: input consumed and current state in $F$.",
        "By empty stack: input consumed and stack empty.",
        "Given an empty-stack PDA, add a final state entered when the stack empties.",
        "Given a final-state PDA, add moves that dump the stack on reaching $F$.",
        "Both modes recognise exactly the context-free languages.",
      ],
      example: "The $0^n1^n$ PDA accepts by empty stack (all $X$s popped). Adding a state reached on empty stack gives an equivalent final-state PDA.",
      misconception: "Empty stack and final state are equivalent in power, not necessarily for the same machine unchanged; you convert between them with a standard gadget.",
      rule: "Accept by final state or by empty stack: same language class (context-free).",
    },
    {
      id: "pda-cfg",
      title: "PDA and CFG Equivalence",
      explainer: "derivation",
      what: "Context-free grammars and pushdown automata are equivalent: every CFG has a PDA for its language, and every PDA's language has a CFG.",
      why: "This is the CFL counterpart of Kleene's theorem (regex and finite automata). It ties the generative view (grammars) to the machine view (PDAs).",
      intuition: "A PDA simulates left-most derivations of a CFG, using its stack to hold the part of the sentential form not yet matched; conversely a PDA's stack behaviour encodes as grammar rules.",
      analogy: {
        icon: "map",
        label: "Grammar equals machine",
        text: "The grammar is the recipe, the PDA is the cook with a stack of notes; each reproduces exactly what the other does.",
        breaks: "They are equal in power, but a given PDA and grammar can be very different in size.",
      },
      steps: [
        "From a CFG, build a PDA that keeps the unexpanded suffix of a derivation on its stack.",
        "Push the start symbol, then repeatedly expand the top variable or match a terminal.",
        "Acceptance by empty stack means the derivation matched the input.",
        "Conversely, encode a PDA's (state, stack) transitions as grammar variables.",
        "Hence CFG and PDA define the same languages: the context-free languages.",
      ],
      example: "The grammar $S \\to 0S1 \\mid \\varepsilon$ and the push-0 / pop-on-1 PDA both define $\\{0^n 1^n\\}$.",
      misconception: "Determinism matters here: deterministic PDAs are strictly weaker than nondeterministic ones, even though for finite automata determinism made no difference.",
      rule: "CFG and PDA are equivalent: both describe exactly the context-free languages.",
    },
  ],
  ch5: [
    {
      id: "csl",
      title: "Context-Sensitive Languages",
      explainer: "chomsky",
      what: "A context-sensitive grammar has non-contracting rules $\\alpha \\to \\beta$ with $|\\alpha| \\le |\\beta|$; its languages sit between context-free and recursively enumerable.",
      why: "Some languages (like $0^n 1^n 2^n$) are beyond context-free but still decidable. Context-sensitive grammars capture exactly this next tier of power.",
      intuition: "Rules may depend on context and can replace a chunk by a longer one, but never shrink the string. That 'never shrink' rule keeps membership decidable.",
      analogy: {
        icon: "stack",
        label: "One tier up",
        text: "Loosen the context-free restriction (single variable on the left) to allow context on both sides, and you climb from $0^n1^n$ up to $0^n1^n2^n$.",
        breaks: "More power costs efficiency: parsing context-sensitive languages is far harder than the linear-time parsing of context-free ones.",
      },
      steps: [
        "Rules $\\alpha \\to \\beta$ with $|\\alpha| \\le |\\beta|$ (non-contracting).",
        "The left side may carry context, not just a single variable.",
        "Generated strings never get shorter during a derivation.",
        "Recognised by linear-bounded automata.",
        "Sits strictly between context-free and recursively enumerable.",
      ],
      example: "$\\{0^n 1^n 2^n\\}$ is context-sensitive but not context-free. $\\{a^n b^n c^n d^n\\}$ is another classic example.",
      misconception: "Context-sensitive is still decidable: a machine can always test membership. It is only unrestricted (Type 0) grammars that reach undecidability.",
      rule: "Context-sensitive: non-contracting rules ($|\\alpha|\\le|\\beta|$); recognised by linear-bounded automata; Type 1 in the hierarchy.",
    },
    {
      id: "lba",
      title: "Linear-Bounded Automata",
      explainer: "turing",
      what: "A linear-bounded automaton is a Turing machine whose tape is limited to the portion holding the input. It recognises exactly the context-sensitive languages.",
      why: "It pins down the machine model for Type 1: just enough memory to revisit the input, but not the unbounded tape of a full Turing machine.",
      intuition: "Take a Turing machine and forbid it from using tape beyond the input. With only linear space it can still check things like equal blocks of $0$s, $1$s and $2$s.",
      analogy: {
        icon: "stack",
        label: "A boxed-in Turing machine",
        text: "Same read/write/move head as a Turing machine, but it cannot run off the end of the input, its scratch space is bounded by the problem size.",
        breaks: "That space bound is exactly what separates context-sensitive (decidable) from the unbounded, sometimes-undecidable Turing machine.",
      },
      steps: [
        "Start from the Turing machine model: tape, head, finite control.",
        "Restrict the usable tape to the cells holding the input.",
        "The head reads, writes and moves only within those bounds.",
        "It accepts exactly the context-sensitive languages.",
        "Bounded space keeps membership decidable.",
      ],
      example: "An LBA can decide $0^n 1^n 2^n$ by repeatedly crossing off one $0$, one $1$ and one $2$ within the input region.",
      misconception: "An LBA is not weaker because it is 'linear' time; it is restricted in SPACE (tape length), which is what bounds its language class.",
      rule: "LBA = Turing machine with input-bounded tape; recognises exactly the context-sensitive languages.",
    },
  ],
  ch6: [
    {
      id: "turing-machine",
      title: "The Turing Machine",
      explainer: "turing",
      what: "A Turing machine is a finite control with an unbounded tape and a head that reads, writes and moves one cell at a time. It is the most powerful model: it captures exactly what is computable.",
      why: "Every other model (and every real computer) is at most as powerful as a Turing machine. It is the yardstick for 'computable' and the top of the Chomsky hierarchy.",
      intuition: "On each step the machine looks at the symbol under the head, writes a symbol, moves left or right, and changes state. Repeat until it halts.",
      analogy: {
        icon: "stack",
        label: "Infinite notebook",
        text: "An endless strip of cells you can read and overwrite, with a pointer you slide one cell at a time, that is enough to run any algorithm.",
        breaks: "Real machines have finite memory; the Turing machine idealises memory as unbounded to define what is computable in principle.",
      },
      steps: [
        "Components: states, tape alphabet, an unbounded tape, a start and halting states.",
        "A transition reads (state, tape symbol) and writes a symbol, moves L or R, and changes state.",
        "The machine halts when it reaches an accepting or rejecting state.",
        "Type 0 (unrestricted) grammars match Turing machines.",
        "Church-Turing thesis: this captures all of effective computation.",
      ],
      example: "A Turing machine can decide $0^n1^n$ by crossing off a $0$ then a $1$ each pass, or compute any function a computer can.",
      misconception: "Turing machines are about what is computable in principle, not speed. Their power is the unbounded tape, not fast hardware.",
      rule: "Turing machine = finite control + unbounded read/write tape; defines exactly the computable.",
    },
    {
      id: "decidability",
      title: "Decidable vs Recognizable",
      explainer: "turing",
      what: "A language is recognizable (recursively enumerable) if a Turing machine accepts its members; it is decidable if a machine halts on every input with the right answer. Decidable is a strict subset of recognizable.",
      why: "Many problems can be confirmed when true but never definitively rejected. The decidable/recognizable split is the precise line between 'always answerable' and 'only confirmable'.",
      intuition: "A recognizer may loop forever on non-members; a decider always halts. The difference is whether the machine is guaranteed to stop and say 'no'.",
      analogy: {
        icon: "stack",
        label: "Always answers vs sometimes",
        text: "A decider is an oracle that always returns yes or no; a recognizer says 'yes' when it should but may hang forever instead of saying 'no'.",
        breaks: "A language and its complement both recognizable implies decidable; the halting problem is recognizable but not decidable.",
      },
      steps: [
        "Recognizable: some Turing machine accepts exactly the members (may loop on non-members).",
        "Decidable: some Turing machine halts on every input, accepting or rejecting.",
        "Every decidable language is recognizable; not conversely.",
        "If both $L$ and its complement are recognizable, $L$ is decidable.",
        "The halting problem is recognizable but not decidable.",
      ],
      example: "Whether a program halts is recognizable (run it and see if it stops) but not decidable (you cannot always conclude it never will).",
      misconception: "Recognizable does not mean solvable. You may confirm membership yet never be able to rule it out, that is the gap from decidable.",
      rule: "Recognizable = accepts members (may loop); decidable = halts on all inputs. Decidable $\\subsetneq$ recognizable.",
    },
    {
      id: "halting-problem",
      title: "The Halting Problem",
      explainer: "halting",
      what: "No Turing machine can decide, for every program and input, whether that program halts. The halting problem is undecidable, proved by diagonalization.",
      why: "It is the foundational impossibility result: a hard limit on what any computer can ever decide, and the source of many other undecidability proofs by reduction.",
      intuition: "Assume a halt-decider exists. Build a machine $D$ that does the opposite of what the decider predicts about $D$ run on itself. Whatever $D$ does contradicts the prediction.",
      analogy: {
        icon: "stack",
        label: "The contradiction machine",
        text: "List every program's behaviour on its own code, then build one that flips the diagonal. It cannot be on the list, yet it is a perfectly good program.",
        breaks: "The proof shows no general decider exists; specific programs can of course still be analysed case by case.",
      },
      steps: [
        "Suppose a decider $H(P, x)$ tells whether program $P$ halts on input $x$.",
        "Build $D(P)$: if $H(P,P)$ says halt, loop; else halt.",
        "Run $D$ on its own code $D$.",
        "If $D$ halts, then by construction it loops; if it loops, it halts.",
        "Contradiction, so $H$ cannot exist: halting is undecidable.",
      ],
      example: "There is no general program that reads any other program and reliably reports whether it will eventually stop.",
      misconception: "Undecidable means no algorithm works for ALL cases, not that every instance is hard. Many specific programs are easy to analyse.",
      rule: "The halting problem is undecidable: by diagonalization, no Turing machine decides halting for all programs.",
    },
  ],
};
